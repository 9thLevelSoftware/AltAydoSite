---
phase: 14-design-system-consolidation
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/auth/LoginForm.tsx
  - src/components/auth/SignupForm.tsx
autonomous: true

must_haves:
  truths:
    - "LoginForm uses MobiGlasInput for all form inputs instead of hand-rolled HTML inputs"
    - "LoginForm uses MobiGlasButton for submit button with isLoading prop"
    - "LoginForm uses MobiGlasFormError for form-level error display"
    - "SignupForm uses MobiGlasInput for all form inputs instead of hand-rolled HTML inputs"
    - "SignupForm uses MobiGlasButton for submit button with isLoading prop"
    - "SignupForm uses MobiGlasFormError for form-level error display"
    - "Auth form redirect logic, callback URL validation, and session management are unchanged"
  artifacts:
    - path: "src/components/auth/LoginForm.tsx"
      provides: "Login form using MobiGlas design system components"
      contains: "MobiGlasInput"
    - path: "src/components/auth/SignupForm.tsx"
      provides: "Signup form using MobiGlas design system components"
      contains: "MobiGlasInput"
  key_links:
    - from: "src/components/auth/LoginForm.tsx"
      to: "@/components/ui/mobiglas"
      via: "import MobiGlasInput, MobiGlasButton, MobiGlasFormError"
      pattern: "import.*MobiGlasInput.*from.*mobiglas"
    - from: "src/components/auth/SignupForm.tsx"
      to: "@/components/ui/mobiglas"
      via: "import MobiGlasInput, MobiGlasButton, MobiGlasFormError"
      pattern: "import.*MobiGlasInput.*from.*mobiglas"
---

<objective>
Migrate LoginForm and SignupForm to use MobiGlas design system components (MobiGlasInput, MobiGlasButton, MobiGlasFormError).

Purpose: DS-04 requires auth forms match the site's visual language. Currently they use hand-rolled inputs with inline Tailwind, raw mg-button CSS, and inconsistent error display. Migration to MobiGlasInput gives built-in corner accents, error display (tier 1), and accessibility (aria-invalid, aria-describedby). MobiGlasFormError provides consistent tier-2 error banners.

Output: LoginForm.tsx and SignupForm.tsx using MobiGlas components for all presentation elements.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-system-consolidation/14-01-SUMMARY.md
@src/components/auth/LoginForm.tsx
@src/components/auth/SignupForm.tsx
@src/components/ui/mobiglas/MobiGlasInput.tsx
@src/components/ui/mobiglas/MobiGlasButton.tsx
@src/components/ui/mobiglas/MobiGlasFormError.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate LoginForm to MobiGlas components</name>
  <files>src/components/auth/LoginForm.tsx</files>
  <action>
  Modify LoginForm.tsx PRESENTATION ONLY — do not touch handleSubmit, isValidCallbackUrl, session management, redirect logic, or Discord OAuth flow.

  **Inputs — Replace hand-rolled inputs with MobiGlasInput:**
  - AydoCorp Handle input -> `<MobiGlasInput label="AYDOCORP HANDLE" id="login-handle" type="text" placeholder="ENTER HANDLE" required autoComplete="username" value={...} onChange={...} />`
  - Password input -> `<MobiGlasInput label="PASSWORD" id="login-password" type="password" placeholder="ENTER PASSWORD" required autoComplete="current-password" value={...} onChange={...} />`
  - Preserve existing state variables (aydoHandle, password) and onChange handlers
  - Remove the hand-rolled corner accent divs that surround each input (MobiGlasInput has built-in corners)
  - Remove the hand-rolled label elements (MobiGlasInput renders labels internally)

  **Submit button — Replace raw mg-button with MobiGlasButton:**
  - Replace `<motion.button className="mg-button ...">` with `<MobiGlasButton type="submit" variant="primary" fullWidth isLoading={isLoading} disabled={isLoading}>ACCESS SYSTEM</MobiGlasButton>`
  - Remove the inline loading text change (MobiGlasButton shows spinner via isLoading)
  - Remove the scanner-line animation div (MobiGlasButton has withScanline if desired)

  **Error display — Replace hand-rolled error with MobiGlasFormError:**
  - Replace the existing error message div (whatever pattern is used) with `<MobiGlasFormError message={error} />` (where `error` is the existing error state variable)
  - Remove any inline error styling

  **Discord OAuth button:**
  - If it uses raw mg-button styling, replace with `<MobiGlasButton variant="secondary" className="discord-specific-classes-if-any">`
  - Preserve the Discord icon and OAuth onClick handler exactly

  **Corner accents around the form container:**
  - If the form wrapper uses inline corner accents, replace with `<CornerAccents size="md" color="primary" />` or wrap in MobiGlasPanel

  **Import updates:**
  - Add: `import { MobiGlasInput, MobiGlasButton } from '@/components/ui/mobiglas'`
  - Add: `import { MobiGlasFormError } from '@/components/ui/mobiglas'`
  - Remove: any motion imports that were only used for the submit button animation (if MobiGlasButton covers it)
  - Keep: motion imports still needed for other animations in the form (page transitions, etc.)

  **CRITICAL:** Do NOT modify:
  - `handleSubmit` function body
  - `isValidCallbackUrl` function
  - `useSession` / `signIn` / `update` calls
  - Redirect logic after successful login
  - Any state management logic
  </action>
  <verify>Run `npm run type-check`. Verify LoginForm imports MobiGlasInput and MobiGlasButton. Verify no raw mg-button class remains.</verify>
  <done>LoginForm uses MobiGlasInput for all inputs, MobiGlasButton for submit with loading spinner, MobiGlasFormError for error display. All security-critical logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate SignupForm to MobiGlas components</name>
  <files>src/components/auth/SignupForm.tsx</files>
  <action>
  Modify SignupForm.tsx PRESENTATION ONLY — same approach as LoginForm.

  **Inputs — Replace with MobiGlasInput:**
  - Handle input -> `<MobiGlasInput label="AYDOCORP HANDLE" id="signup-handle" ...>`
  - Email input -> `<MobiGlasInput label="EMAIL" id="signup-email" type="email" ...>`
  - Password input -> `<MobiGlasInput label="PASSWORD" id="signup-password" type="password" ...>`
  - Confirm password input -> `<MobiGlasInput label="CONFIRM PASSWORD" id="signup-confirm-password" type="password" ...>`
  - Remove hand-rolled corner accents and labels around each input

  **Password match indicator:**
  - SignupForm likely has a password match/strength indicator. PRESERVE this custom logic.
  - If it's a separate div below the confirm password input, keep it outside MobiGlasInput
  - If it provides error feedback, wire it into MobiGlasInput's `error` prop: `error={passwordMismatch ? "Passwords do not match" : undefined}`

  **Submit button — Replace with MobiGlasButton:**
  - `<MobiGlasButton type="submit" variant="primary" fullWidth isLoading={isLoading} disabled={isLoading}>CREATE ACCOUNT</MobiGlasButton>`

  **Error display — Replace with MobiGlasFormError:**
  - `<MobiGlasFormError message={error} />`

  **Corner accents:** Replace inline corner accents with CornerAccents component or MobiGlasPanel.

  **Import updates:** Same as LoginForm task.

  **CRITICAL:** Do NOT modify:
  - `handleSubmit` function body
  - Password validation logic
  - API call to signup endpoint
  - Redirect logic after successful signup
  - Any state management
  </action>
  <verify>Run `npm run type-check`. Verify SignupForm imports MobiGlasInput and MobiGlasButton. Verify no raw mg-button class remains.</verify>
  <done>SignupForm uses MobiGlasInput for all inputs (with password match wired to error prop), MobiGlasButton for submit with loading spinner, MobiGlasFormError for error display. All submission logic unchanged.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. LoginForm and SignupForm import MobiGlasInput, MobiGlasButton, MobiGlasFormError from @/components/ui/mobiglas
3. No raw mg-button CSS class in either auth form
4. No hand-rolled corner accent divs in either auth form (replaced by MobiGlasInput built-in or CornerAccents component)
5. Error display uses MobiGlasFormError with role="alert"
6. Login and signup flows work identically (only presentation changed)
</verification>

<success_criteria>
LoginForm and SignupForm use MobiGlas design system components for all presentation elements. Field-level errors use MobiGlasInput error prop (tier 1). Form-level errors use MobiGlasFormError (tier 2). Submit buttons have loading spinners. Security-critical logic is untouched. TypeScript passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-consolidation/14-04-SUMMARY.md`
</output>
