---
phase: 14-design-system-consolidation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/auth/LoginForm.tsx
  - src/components/auth/SignupForm.tsx
  - src/components/Footer.tsx
  - src/components/landing/AboutSection.tsx
  - src/components/profile/UserProfileContent.tsx
  - src/components/ships/ShipSearchBar.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "LoginForm, SignupForm, Footer, AboutSection, UserProfileContent, and ShipSearchBar use CornerAccents component instead of inline corner divs"
    - "SignupForm confirm password field uses MobiGlasInput with built-in corners instead of hand-rolled input"
    - "No visual regression -- corner sizes, colors, and opacities match the original inline implementations"
  artifacts:
    - path: "src/components/auth/LoginForm.tsx"
      provides: "CornerAccents import and usage replacing 4 inline corner divs"
      contains: "CornerAccents"
    - path: "src/components/auth/SignupForm.tsx"
      provides: "CornerAccents for form container + MobiGlasInput for confirm password"
      contains: "CornerAccents"
    - path: "src/components/Footer.tsx"
      provides: "CornerAccents replacing inline corner divs"
      contains: "CornerAccents"
    - path: "src/components/landing/AboutSection.tsx"
      provides: "CornerAccents replacing inline corner divs"
      contains: "CornerAccents"
    - path: "src/components/profile/UserProfileContent.tsx"
      provides: "CornerAccents replacing inline corner divs"
      contains: "CornerAccents"
    - path: "src/components/ships/ShipSearchBar.tsx"
      provides: "CornerAccents replacing inline corner divs"
      contains: "CornerAccents"
  key_links:
    - from: "LoginForm.tsx, SignupForm.tsx, Footer.tsx, AboutSection.tsx, UserProfileContent.tsx, ShipSearchBar.tsx"
      to: "@/components/ui/mobiglas/CornerAccents"
      via: "import CornerAccents"
      pattern: "import.*CornerAccents"
---

<objective>
Replace inline corner accent divs with CornerAccents component in 6 files, and migrate SignupForm's confirm password input to MobiGlasInput.

Purpose: Close Gap 1 from VERIFICATION.md -- corner accents must use a single CornerAccents component everywhere (Success Criterion 2). HomeContent.tsx is excluded because its decorative glowing corners (bg-based lines with box-shadow glow, inner dots, secondary lines) exceed the CornerAccents component's capabilities and are intentional landing page design elements.

Output: 6 files migrated from inline corner divs to CornerAccents component.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/mobiglas/CornerAccents.tsx
@src/components/ui/mobiglas/MobiGlasInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline corner divs in LoginForm, SignupForm (with MobiGlasInput migration), and Footer</name>
  <files>
    src/components/auth/LoginForm.tsx
    src/components/auth/SignupForm.tsx
    src/components/Footer.tsx
  </files>
  <action>
    **LoginForm.tsx:**
    1. Add import: `import CornerAccents from '@/components/ui/mobiglas/CornerAccents';`
    2. Find the 4 inline corner divs (~lines 165-168) inside the `mg-panel` container:
       ```
       {/* Corner brackets */}
       <div className="absolute top-0 left-0 w-5 h-5 border-l border-t border-[rgba(var(--mg-primary),0.5)]"></div>
       <div className="absolute top-0 right-0 w-5 h-5 border-r border-t border-[rgba(var(--mg-primary),0.5)]"></div>
       <div className="absolute bottom-0 left-0 w-5 h-5 border-l border-b border-[rgba(var(--mg-primary),0.5)]"></div>
       <div className="absolute bottom-0 right-0 w-5 h-5 border-r border-b border-[rgba(var(--mg-primary),0.5)]"></div>
       ```
    3. Replace all 4 divs with: `<CornerAccents size="md" color="primary" opacity="medium" />`
       - `size="md"` = w-5 h-5 (matches original)
       - `color="primary"` = --mg-primary (matches original)
       - `opacity="medium"` = 0.6 (original was 0.5, but 0.6 is the closest available; visually near-identical)

    **SignupForm.tsx:**
    1. Add import: `import CornerAccents from '@/components/ui/mobiglas/CornerAccents';`
    2. Find the 4 inline corner divs (~lines 104-107) inside the form container:
       ```
       {/* Corner brackets */}
       <div className="absolute top-0 left-0 w-5 h-5 border-l border-t border-[rgba(var(--mg-primary),0.5)]"></div>
       ...
       ```
    3. Replace all 4 with: `<CornerAccents size="md" color="primary" opacity="medium" />`
    4. Find the confirm password section (~lines 170-315). The hand-rolled input with inline corner accents (~lines 195-213) should be replaced with a MobiGlasInput component:
       - Replace the raw `<input>` element and its 4 surrounding corner accent divs with `<MobiGlasInput>` which has built-in corners
       - Keep the password match indicator, show/hide toggle, and validation logic
       - Map existing props: `type="password"`, `value={confirmPassword}`, `onChange`, `placeholder`, `autoComplete="new-password"`, `error` for validation message
       - The MobiGlasInput already provides corner accents, field styling, and error display -- no need for the custom wrapper

    **Footer.tsx:**
    1. Add import: `import CornerAccents from '@/components/ui/mobiglas/CornerAccents';`
    2. Find the 4 inline corner divs (~lines 57-60):
       ```
       {/* Minimal corner brackets */}
       <div className="absolute top-0 left-0 w-5 h-5 border-l border-t border-[rgba(var(--mg-primary),0.3)]"></div>
       ...
       ```
    3. Replace all 4 with: `<CornerAccents size="md" color="primary" opacity="low" />`
       - `opacity="low"` = 0.4 (original was 0.3; closest available option)
  </action>
  <verify>
    Run `npm run type-check` to verify no TypeScript errors.
    Grep for inline corner patterns in all 3 files: `grep -n "border-l border-t\|border-r border-t\|border-l border-b\|border-r border-b" src/components/auth/LoginForm.tsx src/components/auth/SignupForm.tsx src/components/Footer.tsx` should return zero matches.
    Grep for CornerAccents import: `grep -n "CornerAccents" src/components/auth/LoginForm.tsx src/components/auth/SignupForm.tsx src/components/Footer.tsx` should show imports in all 3 files.
  </verify>
  <done>LoginForm, SignupForm, and Footer use CornerAccents component. SignupForm confirm password uses MobiGlasInput. Zero inline corner divs remain in these 3 files.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline corner divs in AboutSection, UserProfileContent, and ShipSearchBar</name>
  <files>
    src/components/landing/AboutSection.tsx
    src/components/profile/UserProfileContent.tsx
    src/components/ships/ShipSearchBar.tsx
  </files>
  <action>
    **AboutSection.tsx:**
    1. Add import: `import CornerAccents from '@/components/ui/mobiglas/CornerAccents';`
    2. Find the 4 inline corner divs (~lines 105-108) inside the card overlay:
       ```
       {/* Corner decorations */}
       <div className="absolute top-0 left-0 w-6 h-6 border-l border-t border-[rgba(var(--mg-primary),0.6)]"></div>
       ...
       ```
    3. Replace all 4 with: `<CornerAccents size="sm" color="primary" opacity="medium" />`
       - `size="sm"` = w-3 h-3. BUT original is w-6 h-6 which falls between md (w-5) and lg (w-8). Use `size="lg"` (w-8 h-8) as the closest match that doesn't feel undersized. If visually too large, fall back to `size="md"`.
       - Actually, re-check: sm=w-3, md=w-5, lg=w-8. Original is w-6. Use `size="md"` as the safer default (slightly smaller is less noticeable than larger).
       - `opacity="medium"` = 0.6 (matches original)

    **UserProfileContent.tsx:**
    1. Add import: `import CornerAccents from '@/components/ui/mobiglas/CornerAccents';`
    2. Find the 4 inline corner divs (~lines 352-355):
       ```
       {/* Corner brackets */}
       <div className="absolute top-0 left-0 w-5 h-5 border-l border-t border-[rgba(var(--mg-primary),0.5)]"></div>
       ...
       ```
    3. Replace all 4 with: `<CornerAccents size="md" color="primary" opacity="medium" />`

    **ShipSearchBar.tsx:**
    1. Add import: `import CornerAccents from '@/components/ui/mobiglas/CornerAccents';`
    2. Find the 4 inline corner divs (~lines 91-94):
       ```
       {/* Corner accents */}
       <div className="absolute top-0 left-0 w-[6px] h-[6px] border-l border-t border-[rgba(var(--mg-primary),0.4)] pointer-events-none" />
       ...
       ```
    3. Replace all 4 with: `<CornerAccents size="xs" color="primary" opacity="low" className="pointer-events-none" />`
       - `size="xs"` = w-2 h-2 (original is 6px ~ 0.375rem; xs is 0.5rem -- close enough)
       - `opacity="low"` = 0.4 (matches original)
       - `className="pointer-events-none"` preserves the original pointer-events behavior. NOTE: CornerAccents passes className to each div, so pointer-events-none will apply correctly.
  </action>
  <verify>
    Run `npm run type-check` to verify no TypeScript errors.
    Grep for inline corner patterns in all 3 files: `grep -n "border-l border-t\|border-r border-t" src/components/landing/AboutSection.tsx src/components/profile/UserProfileContent.tsx src/components/ships/ShipSearchBar.tsx` should return zero matches.
    Grep for CornerAccents import: `grep -n "CornerAccents" src/components/landing/AboutSection.tsx src/components/profile/UserProfileContent.tsx src/components/ships/ShipSearchBar.tsx` should show imports in all 3 files.
  </verify>
  <done>AboutSection, UserProfileContent, and ShipSearchBar use CornerAccents component. Zero inline corner divs remain in these 3 files. HomeContent.tsx decorative glowing corners are intentionally excluded (they use bg-based glow effects, inner dots, and secondary lines that exceed CornerAccents' simple/detailed/animated variants).</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes with zero errors in all 6 modified files
2. No inline corner accent divs (w-5 h-5 border-l border-t pattern) remain in any of the 6 files
3. All 6 files import and use CornerAccents from `@/components/ui/mobiglas/CornerAccents`
4. SignupForm confirm password field uses MobiGlasInput instead of hand-rolled input with manual corners
5. `npm run build` succeeds
</verification>

<success_criteria>
- 6 of 7 identified files migrated from inline corner divs to CornerAccents component
- HomeContent.tsx excluded (decorative glowing corners are intentional design -- bg-based lines with box-shadow glow effects, inner pulsing dots, and secondary lines that exceed CornerAccents' capabilities)
- SignupForm confirm password uses MobiGlasInput
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-consolidation/14-08-SUMMARY.md`
</output>
