---
phase: 14-design-system-consolidation
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/home/Footer.tsx
  - src/components/home/HomeContent.tsx
  - src/components/home/HeroSection.tsx
  - src/app/services/page.tsx
  - src/app/recruitment/page.tsx
  - src/components/contact/ContactForm.tsx
  - src/components/fleet-ops/finance/FinanceTrackerClient.tsx
  - src/components/auth/AuthError.tsx
  - src/components/reference/ReferencePageContent.tsx
autonomous: true

must_haves:
  truths:
    - "Footer uses MobiGlasButton instead of raw mg-button CSS class"
    - "HomeContent and HeroSection use MobiGlasButton instead of raw mg-button CSS"
    - "ContactForm uses MobiGlasButton with isLoading instead of raw mg-button"
    - "Services and recruitment pages use MobiGlasButton instead of raw mg-button"
    - "FinanceTrackerClient, AuthError, and ReferencePageContent use MobiGlasButton"
  artifacts:
    - path: "src/components/home/Footer.tsx"
      provides: "Footer with MobiGlasButton"
      contains: "MobiGlasButton"
    - path: "src/components/contact/ContactForm.tsx"
      provides: "Contact form with MobiGlasButton + isLoading"
      contains: "isLoading"
  key_links:
    - from: "src/components/contact/ContactForm.tsx"
      to: "@/components/ui/mobiglas"
      via: "import MobiGlasButton"
      pattern: "import.*MobiGlasButton.*from.*mobiglas"
---

<objective>
Replace all raw mg-button CSS usage in public pages and non-fleet-ops components with MobiGlasButton.

Purpose: Eliminates the remaining ~20 raw mg-button usages across public pages, completing DS-02 button consolidation. ContactForm gains a proper loading spinner (UX-07). After this plan, only auth forms still use raw mg-button (handled by Plan 04).

Output: All non-auth, non-fleet-ops components using MobiGlasButton.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-system-consolidation/14-01-SUMMARY.md
@src/components/ui/mobiglas/MobiGlasButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw mg-button in home page components</name>
  <files>src/components/home/Footer.tsx, src/components/home/HomeContent.tsx, src/components/home/HeroSection.tsx</files>
  <action>
  For each file, replace ALL raw `<button className="mg-button ...">` or `<motion.button className="mg-button ...">` elements with `<MobiGlasButton>`:

  **Footer.tsx:**
  - Replace footer action buttons with MobiGlasButton
  - Use appropriate variant (likely primary or ghost depending on button purpose)
  - Add MobiGlasButton import from `@/components/ui/mobiglas`

  **HomeContent.tsx:**
  - Replace CTA buttons with MobiGlasButton
  - For navigation/link-style buttons, use variant="primary" or variant="accent"
  - Preserve any motion animation props by passing through (MobiGlasButton extends MotionProps)
  - If buttons use `<Link>` wrappers, keep the Link but replace the inner button styling

  **HeroSection.tsx:**
  - Replace hero CTA buttons with MobiGlasButton
  - Use variant="accent" or variant="primary" for prominent CTAs
  - Add `withGlow` for hero-level emphasis if appropriate

  **Rules for all replacements:**
  - Preserve existing onClick handlers, href wrappers, and children content
  - If the raw button uses `whileTap={{ scale: 0.98 }}`, REMOVE it (MobiGlasButton has this built-in)
  - If the raw button has custom background colors via Tailwind, map to the closest MobiGlasButton variant
  - Add `size="lg"` for hero/CTA buttons that were larger
  - Do NOT modify any non-button JSX or business logic
  </action>
  <verify>Run `npm run type-check`. Grep for "mg-button" in these 3 files to confirm zero remaining raw usages.</verify>
  <done>Footer, HomeContent, and HeroSection use MobiGlasButton exclusively. No raw mg-button CSS class in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw mg-button in remaining public and utility components</name>
  <files>src/app/services/page.tsx, src/app/recruitment/page.tsx, src/components/contact/ContactForm.tsx, src/components/fleet-ops/finance/FinanceTrackerClient.tsx, src/components/auth/AuthError.tsx, src/components/reference/ReferencePageContent.tsx</files>
  <action>
  For each file, replace ALL raw mg-button CSS usage with MobiGlasButton:

  **services/page.tsx and recruitment/page.tsx:**
  - Replace CTA/action buttons with MobiGlasButton variant="primary" or variant="accent"
  - Preserve Link wrappers if present

  **ContactForm.tsx:**
  - Replace submit button with `MobiGlasButton type="submit" variant="primary" isLoading={isLoading} disabled={isLoading} fullWidth`
  - This adds the loading spinner that was missing (UX-07)
  - Preserve the existing isLoading state variable and form submission logic

  **FinanceTrackerClient.tsx:**
  - Replace action buttons with appropriate MobiGlasButton variants
  - Finance-related actions: primary for add, danger for delete

  **AuthError.tsx:**
  - Replace error page action button (likely "Return to login" or similar) with MobiGlasButton variant="primary"

  **ReferencePageContent.tsx:**
  - Replace any navigation/filter buttons with MobiGlasButton variant="ghost" or variant="outline"

  **Rules for all replacements:**
  - Import MobiGlasButton from `@/components/ui/mobiglas`
  - Remove redundant `whileTap`/`whileHover` (built into MobiGlasButton)
  - Map button sizing: small buttons -> size="sm", default -> size="md", large -> size="lg"
  - Do NOT modify non-button JSX or business logic
  </action>
  <verify>Run `npm run type-check`. Grep for `className="mg-button` or `className={\`mg-button` across all modified files to confirm zero remaining raw usages.</verify>
  <done>All non-auth, non-fleet-ops components use MobiGlasButton. ContactForm has loading spinner. No TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. Grep for raw mg-button in modified files returns zero results
3. ContactForm submit button uses MobiGlasButton with isLoading prop
4. All 9 files import MobiGlasButton from @/components/ui/mobiglas
</verification>

<success_criteria>
All public pages and utility components use MobiGlasButton exclusively. ContactForm has a proper loading spinner (UX-07 partially addressed). Raw mg-button CSS class no longer used in any non-auth component. TypeScript passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-consolidation/14-03-SUMMARY.md`
</output>
