---
phase: 14-design-system-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/fleet-ops/mission-planner/MissionDashboard.tsx
  - src/components/fleet-ops/mission-planner/HolographicButton.tsx
  - src/components/fleet-ops/operations/OperationDetailView.tsx
  - src/components/fleet-ops/operations/OperationCard.tsx
autonomous: true

must_haves:
  truths:
    - "MissionDashboard uses MobiGlasButton instead of HolographicButton for all 3 button instances"
    - "OperationDetailView uses MobiGlasButton instead of raw mg-button-primary/mg-button-secondary/mg-button-danger classes"
    - "OperationCard uses MobiGlasButton instead of raw mg-button CSS class"
    - "HolographicButton.tsx is deleted from the codebase"
  artifacts:
    - path: "src/components/fleet-ops/mission-planner/MissionDashboard.tsx"
      provides: "Mission dashboard with MobiGlasButton"
      contains: "MobiGlasButton"
    - path: "src/components/fleet-ops/operations/OperationDetailView.tsx"
      provides: "Operation detail with MobiGlasButton replacing undefined CSS classes"
      contains: "MobiGlasButton"
  key_links:
    - from: "src/components/fleet-ops/mission-planner/MissionDashboard.tsx"
      to: "@/components/ui/mobiglas"
      via: "import MobiGlasButton"
      pattern: "import.*MobiGlasButton.*from.*mobiglas"
---

<objective>
Replace HolographicButton and raw mg-button CSS usage in fleet-ops components with MobiGlasButton.

Purpose: Eliminates 2 of the 3 button implementations in fleet-ops. HolographicButton (3 usages) is redundant now that MobiGlasButton has withCorners/withScanline/withGlow props. OperationDetailView uses undefined CSS classes (mg-button-primary, mg-button-danger) — a pre-existing bug that this plan fixes by switching to MobiGlasButton with proper variants.

Output: Fleet-ops components using MobiGlasButton exclusively, HolographicButton.tsx deleted.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-system-consolidation/14-01-SUMMARY.md
@src/components/fleet-ops/mission-planner/HolographicButton.tsx
@src/components/fleet-ops/mission-planner/MissionDashboard.tsx
@src/components/fleet-ops/operations/OperationDetailView.tsx
@src/components/fleet-ops/operations/OperationCard.tsx
@src/components/ui/mobiglas/MobiGlasButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace HolographicButton with MobiGlasButton in MissionDashboard</name>
  <files>src/components/fleet-ops/mission-planner/MissionDashboard.tsx, src/components/fleet-ops/mission-planner/HolographicButton.tsx</files>
  <action>
  1. In MissionDashboard.tsx, replace all 3 HolographicButton usages with MobiGlasButton:
     - Map HolographicButton variants to MobiGlasButton: primary->primary, secondary->secondary, danger->danger, success->success
     - Add `withCorners withScanline withGlow` props to replicate HolographicButton's visual richness
     - Preserve all existing onClick handlers, disabled states, and children text
     - Replace the import: remove HolographicButton, add MobiGlasButton from `@/components/ui/mobiglas`

  2. Delete `src/components/fleet-ops/mission-planner/HolographicButton.tsx` entirely.

  3. Verify no other files import HolographicButton (research says only MissionDashboard uses it — confirm with grep before deleting).

  Do NOT modify any business logic, state management, or non-button JSX in MissionDashboard.
  </action>
  <verify>Run `npm run type-check`. Grep for "HolographicButton" to confirm zero remaining usages.</verify>
  <done>MissionDashboard uses MobiGlasButton for all buttons. HolographicButton.tsx is deleted. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw mg-button CSS in OperationDetailView and OperationCard</name>
  <files>src/components/fleet-ops/operations/OperationDetailView.tsx, src/components/fleet-ops/operations/OperationCard.tsx</files>
  <action>
  1. In OperationDetailView.tsx, replace ALL raw button elements using mg-button/mg-button-primary/mg-button-secondary/mg-button-danger CSS classes with MobiGlasButton components:
     - `mg-button-primary` (undefined CSS class) -> `MobiGlasButton variant="primary"`
     - `mg-button-secondary` -> `MobiGlasButton variant="secondary"`
     - `mg-button-danger` (undefined CSS class) -> `MobiGlasButton variant="danger"`
     - `mg-button` (plain) -> `MobiGlasButton variant="primary"` or appropriate variant based on context
     - Preserve all onClick handlers, disabled states, and children
     - Add MobiGlasButton import from `@/components/ui/mobiglas`

  2. In OperationCard.tsx, replace any raw mg-button usage with MobiGlasButton:
     - Map button purpose to appropriate variant (action buttons -> primary, delete -> danger)
     - Preserve all existing behavior

  This fixes the pre-existing bug where mg-button-primary and mg-button-danger had NO CSS definitions, meaning those buttons were effectively unstyled.

  Do NOT modify any business logic, API calls, or non-button JSX.
  </action>
  <verify>Run `npm run type-check`. Grep for "mg-button-primary" and "mg-button-danger" to confirm zero remaining usages of undefined CSS classes.</verify>
  <done>OperationDetailView and OperationCard use MobiGlasButton exclusively. Pre-existing bug with undefined mg-button-primary/mg-button-danger CSS classes is fixed. No TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. `grep -r "HolographicButton" src/` returns zero results
3. `grep -r "mg-button-primary\|mg-button-danger" src/` returns zero results in TSX files
4. HolographicButton.tsx file no longer exists
5. All fleet-ops buttons render with MobiGlasButton
</verification>

<success_criteria>
Fleet-ops components (MissionDashboard, OperationDetailView, OperationCard) exclusively use MobiGlasButton. HolographicButton is deleted. Pre-existing unstyled button bug is fixed. TypeScript type checking passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-consolidation/14-02-SUMMARY.md`
</output>
