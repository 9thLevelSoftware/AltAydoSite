---
phase: 14-design-system-consolidation
plan: 05
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/fleet-ops/missions/MissionCard.tsx
  - src/components/fleet-ops/missions/MissionDetail.tsx
  - src/components/fleet-ops/missions/MissionFilters.tsx
  - src/components/fleet-ops/mission-planner/MissionDashboard.tsx
  - src/components/fleet-ops/operations/OperationCard.tsx
  - src/app/forgot-password/page.tsx
  - src/app/reset-password/page.tsx
autonomous: true

must_haves:
  truths:
    - "MissionCard status badge colors use MobiGlas CSS variables (--mg-primary, --mg-success, --mg-danger, --mg-text) instead of hardcoded Tailwind colors"
    - "Inline corner accent divs in MissionDetail, MissionFilters, MissionDashboard, and OperationCard are replaced with CornerAccents component"
    - "Inline corner accent divs in forgot-password and reset-password pages are replaced with CornerAccents component or MobiGlasInput"
  artifacts:
    - path: "src/components/fleet-ops/missions/MissionCard.tsx"
      provides: "Mission card with MobiGlas palette status colors"
      contains: "--mg-primary"
    - path: "src/components/fleet-ops/missions/MissionDetail.tsx"
      provides: "Mission detail with CornerAccents component"
      contains: "CornerAccents"
  key_links:
    - from: "src/components/fleet-ops/missions/MissionCard.tsx"
      to: "globals.css"
      via: "CSS variable references"
      pattern: "--mg-(primary|success|danger|text)"
---

<objective>
Consolidate corner accent patterns to use CornerAccents component and migrate MissionCard status colors to MobiGlas palette variables.

Purpose: DS-03 eliminates 4+ competing corner accent patterns. DS-06 aligns MissionCard status badges with the MobiGlas color system. After this plan, corner accents use a single component and status colors use design system variables.

Output: Corner accents consolidated in fleet-ops and password pages. MissionCard uses --mg-primary/--mg-success/--mg-danger/--mg-text for status colors.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/mobiglas/CornerAccents.tsx
@src/components/fleet-ops/missions/MissionCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate MissionCard status colors to MobiGlas palette variables</name>
  <files>src/components/fleet-ops/missions/MissionCard.tsx</files>
  <action>
  Replace hardcoded Tailwind color classes in the status badge function/mapping with MobiGlas CSS variables:

  **Status color mapping:**
  - Planning: `border-blue-400 text-blue-400 bg-[rgba(59,130,246,0.1)]` -> `border-[rgba(var(--mg-primary),0.5)] text-[rgba(var(--mg-primary),0.8)] bg-[rgba(var(--mg-primary),0.1)]`
  - Briefing: `border-purple-400 text-purple-400 ...` -> `border-[rgba(var(--mg-accent),0.5)] text-[rgba(var(--mg-accent),0.8)] bg-[rgba(var(--mg-accent),0.1)]`
  - In Progress: `border-green-400 text-green-400 ...` -> `border-[rgba(var(--mg-success),0.5)] text-[rgba(var(--mg-success),0.8)] bg-[rgba(var(--mg-success),0.1)]`
  - Completed: `border-gray-400 text-gray-400 ...` -> `border-[rgba(var(--mg-text),0.3)] text-[rgba(var(--mg-text),0.5)] bg-[rgba(var(--mg-text),0.05)]`
  - Archived: `border-gray-500 text-gray-500 ...` -> `border-[rgba(var(--mg-text),0.2)] text-[rgba(var(--mg-text),0.4)] bg-[rgba(var(--mg-text),0.03)]`
  - Cancelled: `border-red-400 text-red-400 ...` -> `border-[rgba(var(--mg-danger),0.5)] text-[rgba(var(--mg-danger),0.8)] bg-[rgba(var(--mg-danger),0.1)]`

  Also replace any inline corner accent divs in MissionCard with CornerAccents component (research shows `w-[20px] h-[20px]` with 2px bars pattern). Use `<CornerAccents size="lg" color="primary" opacity="low" />` — the lg size (w-8 h-8) is the closest match to w-[20px].

  Import CornerAccents from `@/components/ui/mobiglas` if not already imported.

  Do NOT modify any business logic, click handlers, or data display.
  </action>
  <verify>Run `npm run type-check`. Grep for `border-blue-400|border-purple-400|border-green-400|border-red-400` in MissionCard.tsx to confirm zero hardcoded colors remain.</verify>
  <done>MissionCard status colors use MobiGlas CSS variables. Corner accents use CornerAccents component. No hardcoded Tailwind color classes for status badges.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline corner accents in fleet-ops and password pages</name>
  <files>src/components/fleet-ops/missions/MissionDetail.tsx, src/components/fleet-ops/missions/MissionFilters.tsx, src/components/fleet-ops/operations/OperationCard.tsx, src/app/forgot-password/page.tsx, src/app/reset-password/page.tsx</files>
  <action>
  For each file, find and replace inline corner accent patterns with CornerAccents component:

  **Pattern recognition — replace these inline div groups:**
  1. `w-[6px] h-[6px]` corner divs -> `<CornerAccents size="xs" color="primary" opacity="medium" />`
  2. `w-5 h-5` corner divs -> `<CornerAccents size="md" color="primary" opacity="medium" />`
  3. `w-[15px] h-[15px]` with 2px bars -> `<CornerAccents size="sm" color="primary" opacity="medium" />`
  4. `w-[20px] h-[20px]` with 2px bars -> `<CornerAccents size="lg" color="primary" opacity="low" />`

  **For each replacement:**
  - Ensure the parent element has `className="relative ..."` (required for absolute-positioned CornerAccents)
  - Remove all 4 inline corner `<div>` elements (top-left, top-right, bottom-left, bottom-right)
  - Add single `<CornerAccents ... />` inside the parent
  - Match the color to the original border color:
    - `--mg-primary` or cyan -> color="primary"
    - `--mg-accent` -> color="accent"
    - `--mg-success` -> color="success"
  - Match opacity: 0.3-0.4 -> opacity="low", 0.5-0.6 -> opacity="medium", 0.7-0.8 -> opacity="high"

  **forgot-password/page.tsx and reset-password/page.tsx:**
  - Also replace hand-rolled inputs with MobiGlasInput if they use inline corner accents (these pages likely have the same pattern as LoginForm)
  - Replace raw mg-button submit buttons with MobiGlasButton

  Import CornerAccents from `@/components/ui/mobiglas`.

  Do NOT modify non-corner JSX, business logic, or API calls.
  Note: MissionDashboard corner accents may already be handled by Plan 02 (HolographicButton removal). Only replace remaining inline patterns.
  </action>
  <verify>Run `npm run type-check`. Grep for `w-\[6px\] h-\[6px\]|w-\[15px\] h-\[15px\]|w-\[20px\] h-\[20px\]` in modified files to confirm inline corner patterns are eliminated.</verify>
  <done>All inline corner accent divs in fleet-ops components and password pages are replaced with CornerAccents component. Password pages use MobiGlasInput and MobiGlasButton. TypeScript passes.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. MissionCard status colors reference `--mg-primary`, `--mg-success`, `--mg-danger`, `--mg-text` instead of hardcoded Tailwind colors
3. No inline corner accent divs (w-[6px], w-[15px], w-[20px] patterns) remain in modified files
4. CornerAccents component imported and used in all modified files
5. forgot-password and reset-password pages use MobiGlas components
</verification>

<success_criteria>
MissionCard status badges use MobiGlas palette variables (DS-06 complete). Inline corner accent patterns in fleet-ops and password pages are consolidated to CornerAccents component (DS-03 partially complete). TypeScript passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-consolidation/14-05-SUMMARY.md`
</output>
