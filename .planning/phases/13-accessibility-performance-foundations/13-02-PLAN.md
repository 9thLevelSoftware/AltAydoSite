---
phase: 13-accessibility-performance-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/auth/LoginForm.tsx
  - src/components/auth/SignupForm.tsx
  - src/components/dashboard/widgets/TransactionModal.tsx
  - src/components/dashboard/MissionPlannerForm.tsx
  - src/components/dashboard/MissionTemplateForm.tsx
  - src/components/fleet-ops/OperationEditor.tsx
  - src/components/fleet-ops/mission-planner/MissionFilters.tsx
  - src/components/profile/UserProfileContent.tsx
  - src/components/UserProfilePanel.tsx
  - src/components/ships/ShipFilterPanel.tsx
  - src/components/ships/ShipSearchBar.tsx
  - src/app/forgot-password/page.tsx
  - src/app/reset-password/page.tsx
  - src/app/dashboard/subsidiaries/security/page.tsx
autonomous: true
must_haves:
  truths:
    - "Every form input has an associated label with htmlFor/id pairing"
    - "Screen readers announce field purpose when user focuses any input"
    - "Required fields have aria-required attribute"
  artifacts:
    - path: "src/components/auth/LoginForm.tsx"
      provides: "Accessible login form with label associations"
      contains: "htmlFor"
    - path: "src/components/auth/SignupForm.tsx"
      provides: "Accessible signup form with label associations"
      contains: "htmlFor"
    - path: "src/components/dashboard/widgets/TransactionModal.tsx"
      provides: "Accessible transaction modal with label associations"
      contains: "htmlFor"
  key_links:
    - from: "label[htmlFor]"
      to: "input[id]"
      via: "matching htmlFor/id values"
      pattern: "htmlFor.*id="
---

<objective>
Add htmlFor/id label associations to all form inputs across 14 files (~77 label-input pairs), following the MobiGlasInput pattern as the gold standard.

Purpose: Addresses UX-04 -- screen readers must announce field purpose when users focus any input. This is the largest mechanical change in the phase.
Output: All 14 form files updated with proper label-input associations
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/mobiglas/MobiGlasInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add label associations to auth, dashboard, and fleet-ops forms (7 files)</name>
  <files>
    src/components/auth/LoginForm.tsx
    src/components/auth/SignupForm.tsx
    src/components/dashboard/widgets/TransactionModal.tsx
    src/components/dashboard/MissionPlannerForm.tsx
    src/components/dashboard/MissionTemplateForm.tsx
    src/components/fleet-ops/OperationEditor.tsx
    src/components/fleet-ops/mission-planner/MissionFilters.tsx
  </files>
  <action>
  For each file, apply the MobiGlasInput pattern:

  **Pattern to follow (from MobiGlasInput.tsx):**
  ```tsx
  // For each label/input pair without htmlFor/id:
  const fieldId = `form-fieldname`; // Use a stable, descriptive ID
  // If multiple instances of same form possible on page, use useId() or component-scoped prefix

  <label htmlFor={fieldId}>{labelText}</label>
  <input id={fieldId} aria-required={isRequired} />
  ```

  **Per-file specifics:**

  1. **LoginForm.tsx** (~2 labels): Add `htmlFor`/`id` to email/username and password fields. Add `aria-required={true}` to both.

  2. **SignupForm.tsx** (~6 labels): Add `htmlFor`/`id` to all fields (username, email, password, confirm password, handle, etc.). Add `aria-required` to required fields. If error messages exist, add `aria-describedby` pointing to error element IDs.

  3. **TransactionModal.tsx** (~4 labels): Add `htmlFor`/`id` to amount, type/category select, description, and date fields.

  4. **MissionPlannerForm.tsx** (~10 labels): Add `htmlFor`/`id` to all mission planning fields (title, description, type, date/time, location, etc.). Use descriptive IDs like `mission-title`, `mission-description`, etc.

  5. **MissionTemplateForm.tsx** (~10 labels): Same pattern as MissionPlannerForm but for template fields. Use `template-` prefix for IDs to avoid conflicts.

  6. **OperationEditor.tsx** (4 labels at lines ~399, 434, 447, 479): Add `htmlFor`/`id` to the 4 label-input pairs identified in research.

  7. **MissionFilters.tsx** (3 labels): Add `htmlFor`/`id` to filter dropdowns/inputs (status, type, date range).

  **Rules:**
  - Use stable string IDs (not `Math.random()`) -- these forms don't render multiple instances
  - For `<select>` elements, `htmlFor`/`id` works the same as `<input>`
  - For `<textarea>` elements, `htmlFor`/`id` works the same as `<input>`
  - Add `aria-required={true}` to fields that are required for form submission
  - Do NOT modify fields that already use MobiGlasInput component (those already have proper associations)
  </action>
  <verify>
  - `npm run type-check` passes
  - Grep each file for `htmlFor` -- should find at least the expected count
  - Grep each file for `aria-required` on required fields
  - No duplicate IDs within any single file
  </verify>
  <done>
  - LoginForm: 2 label-input pairs with htmlFor/id
  - SignupForm: 6 label-input pairs with htmlFor/id
  - TransactionModal: 4 label-input pairs with htmlFor/id
  - MissionPlannerForm: ~10 label-input pairs with htmlFor/id
  - MissionTemplateForm: ~10 label-input pairs with htmlFor/id
  - OperationEditor: 4 label-input pairs with htmlFor/id
  - MissionFilters: 3 label-input pairs with htmlFor/id
  </done>
</task>

<task type="auto">
  <name>Task 2: Add label associations to profile, ships, and remaining forms (7 files)</name>
  <files>
    src/components/profile/UserProfileContent.tsx
    src/components/UserProfilePanel.tsx
    src/components/ships/ShipFilterPanel.tsx
    src/components/ships/ShipSearchBar.tsx
    src/app/forgot-password/page.tsx
    src/app/reset-password/page.tsx
    src/app/dashboard/subsidiaries/security/page.tsx
  </files>
  <action>
  Same MobiGlasInput pattern as Task 1. Per-file specifics:

  1. **UserProfileContent.tsx** (~8 labels): Profile editing fields (display name, handle, bio, timezone, gameplay loops, etc.). Use `profile-` prefix for IDs.

  2. **UserProfilePanel.tsx** (~7 labels): User panel fields. Use `panel-` prefix for IDs if on same page as UserProfileContent to avoid ID collisions.

  3. **ShipFilterPanel.tsx** (4 labels): Ship filter dropdowns (manufacturer, size, status, etc.). Use `ship-filter-` prefix.

  4. **ShipSearchBar.tsx** (1 label): Search input. ID: `ship-search`. If the label is visually hidden, add `aria-label` on the input as well.

  5. **forgot-password/page.tsx** (1 label): Email input. ID: `forgot-email`. Add `aria-required={true}`.

  6. **reset-password/page.tsx** (2 labels): New password + confirm password. IDs: `reset-password`, `reset-confirm`. Add `aria-required={true}`.

  7. **security/page.tsx** (~15 labels): Security settings page. This has the most labels. Use `security-` prefix for all IDs. Audit each label-input pair carefully. Some may be read-only display fields that don't need label associations -- skip those.

  **Rules (same as Task 1):**
  - Stable string IDs, not random
  - `aria-required` on required fields
  - Don't modify MobiGlasInput usages
  - Don't add htmlFor to labels that aren't associated with an interactive input (e.g., labels used as section headers)
  </action>
  <verify>
  - `npm run type-check` passes
  - `npm run build` passes
  - Grep each file for `htmlFor` -- should find expected counts
  - Final count across all 14 files: ~77 label-input associations total
  </verify>
  <done>
  - All 14 files have proper htmlFor/id label-input associations
  - Required fields have aria-required attribute
  - No duplicate IDs across forms that could coexist on the same page
  - npm run build passes
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `npm run type-check` passes
3. Grep across all 14 files: every `<label>` has a `htmlFor` attribute (except labels used as visual section headers with no input)
4. Grep for orphaned labels: no `<label>` without `htmlFor` in the 14 target files
5. No duplicate `id` values within any single file
</verification>

<success_criteria>
- Every form input across the 14 files has an associated `<label htmlFor="...">` with matching `id` on the input
- Required fields have `aria-required={true}`
- Screen readers announce field purpose when any input receives focus
- Build passes with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-accessibility-performance-foundations/13-02-SUMMARY.md`
</output>
