---
phase: 12-motion-v12-migration
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/fleet-ops/mission-planner/MissionList.tsx
  - src/components/fleet-ops/mission-planner/MissionFilters.tsx
  - src/components/fleet-ops/mission-planner/MissionDetail.tsx
  - src/components/fleet-ops/mission-planner/MissionDashboard.tsx
  - src/components/dashboard/DashboardPanelLayout.tsx
autonomous: true

must_haves:
  truths:
    - "Zero staggerChildren references remain in the codebase"
    - "All animations work correctly -- stagger timing behaves identically to before"
    - "npm run build succeeds with zero errors across all 112 migrated files"
    - "No framer-motion references remain anywhere in src/"
  artifacts:
    - path: "src/components/fleet-ops/mission-planner/MissionList.tsx"
      provides: "stagger() function for child animation delay"
      contains: "stagger"
    - path: "src/components/dashboard/DashboardPanelLayout.tsx"
      provides: "stagger() function for panel layout animation"
      contains: "stagger"
  key_links:
    - from: "5 stagger-migrated files"
      to: "motion/react"
      via: "stagger import"
      pattern: "import.*stagger.*from.*motion/react"
---

<objective>
Migrate deprecated staggerChildren usage to the modern stagger() function in 5 files, then validate the entire Phase 12 migration with a full production build.

Purpose: Eliminate deprecated API usage that would produce console warnings and will eventually break in a future motion release. Validate the complete migration end-to-end.
Output: 5 files updated with stagger() function, full build passing, zero framer-motion or staggerChildren references remaining.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-motion-v12-migration/12-RESEARCH.md
@.planning/phases/12-motion-v12-migration/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate staggerChildren to stagger() function in 5 files</name>
  <files>
    src/components/fleet-ops/mission-planner/MissionList.tsx,
    src/components/fleet-ops/mission-planner/MissionFilters.tsx,
    src/components/fleet-ops/mission-planner/MissionDetail.tsx,
    src/components/fleet-ops/mission-planner/MissionDashboard.tsx,
    src/components/dashboard/DashboardPanelLayout.tsx
  </files>
  <action>
For each of the 5 files, perform two changes:

**Change 1: Add `stagger` to the import statement.**

Each file already imports from `'motion/react'` (done in Plan 12-01). Add `stagger` to the existing named imports. For example:
- `import { motion } from 'motion/react'` becomes `import { motion, stagger } from 'motion/react'`
- `import { motion, AnimatePresence } from 'motion/react'` becomes `import { motion, AnimatePresence, stagger } from 'motion/react'`

**Change 2: Replace `staggerChildren: N` with `delayChildren: stagger(N)` in variant transition objects.**

The exact patterns in each file:

1. **MissionList.tsx** (line ~27):
   ```
   // Before:
   staggerChildren: 0.1
   // After:
   delayChildren: stagger(0.1)
   ```

2. **MissionFilters.tsx** (line ~36):
   ```
   // Before:
   staggerChildren: 0.1
   // After:
   delayChildren: stagger(0.1)
   ```

3. **MissionDetail.tsx** (line ~53):
   ```
   // Before:
   staggerChildren: 0.1
   // After:
   delayChildren: stagger(0.1)
   ```

4. **MissionDashboard.tsx** (line ~53):
   ```
   // Before:
   staggerChildren: 0.1
   // After:
   delayChildren: stagger(0.1)
   ```

5. **DashboardPanelLayout.tsx** (line ~17):
   ```
   // Before:
   staggerChildren: 0.1,
   delayChildren: 0.1,
   // After:
   delayChildren: stagger(0.1, { startDelay: 0.1 }),
   ```
   NOTE: This file has BOTH `staggerChildren` AND `delayChildren` already. The `delayChildren: 0.1` represents a start delay before staggering begins. Combine them using stagger()'s `startDelay` option: `delayChildren: stagger(0.1, { startDelay: 0.1 })`. Remove the separate `staggerChildren` line entirely.

After all changes, verify zero `staggerChildren` references remain:
```bash
grep -r "staggerChildren" src/
```
Must return nothing.
  </action>
  <verify>
- `grep -r "staggerChildren" src/` returns zero results
- `grep -r "stagger(" src/ | wc -l` returns 5 (one per file)
- `npm run type-check` passes (stagger import resolves, type signature correct)
  </verify>
  <done>All 5 files use stagger() function instead of deprecated staggerChildren; type-check passes</done>
</task>

<task type="auto">
  <name>Task 2: Full production build validation</name>
  <files>package.json</files>
  <action>
Run the complete validation suite for the entire Phase 12 migration:

1. Verify no framer-motion references remain anywhere:
   ```bash
   grep -r "framer-motion" src/
   ```
   Must return nothing.

2. Verify no staggerChildren references remain:
   ```bash
   grep -r "staggerChildren" src/
   ```
   Must return nothing.

3. Verify no dual-package bloat:
   ```bash
   npm ls framer-motion
   ```
   Must return empty/not found.

4. Verify import count:
   ```bash
   grep -rc "from 'motion/react'" src/ | grep -v ":0$" | wc -l
   ```
   Must return 112 (all files importing from motion/react).

5. Run type-check:
   ```bash
   npm run type-check
   ```
   Must pass. This validates:
   - All 112 import paths resolve to motion/react
   - MotionProps type works in 3 MobiGlas files
   - LazyMotion and domMax types correct in providers
   - stagger() function type signature correct in 5 files

6. Run full production build:
   ```bash
   npm run build
   ```
   Must succeed. This is the definitive validation that the entire migration compiles.

If MotionProps causes type errors in MobiGlasPanel.tsx, MobiGlasContainer.tsx, or MobiGlasButton.tsx:
- Replace `import { motion, MotionProps } from 'motion/react'` with `import { motion, type HTMLMotionProps } from 'motion/react'`
- Replace `MotionProps` usage with `HTMLMotionProps<'div'>` (or the appropriate element type)
- Re-run type-check and build
  </action>
  <verify>
- `npm run type-check` passes
- `npm run build` succeeds (all pages compile)
- `grep -r "framer-motion" src/` returns nothing
- `grep -r "staggerChildren" src/` returns nothing
  </verify>
  <done>Full production build passes; zero framer-motion references; zero deprecated staggerChildren usage; complete Phase 12 migration validated</done>
</task>

</tasks>

<verification>
1. `grep -r "framer-motion" src/` -- zero results (no old package references)
2. `grep -r "staggerChildren" src/` -- zero results (no deprecated API usage)
3. `npm ls framer-motion` -- empty (no dual-package bloat)
4. `npm run type-check` -- passes (all types resolve)
5. `npm run build` -- succeeds (full production validation)
6. `grep -rc "from 'motion/react'" src/ | grep -v ":0$" | wc -l` -- returns 112
</verification>

<success_criteria>
- Zero staggerChildren references in the codebase
- All 5 files use stagger() from motion/react
- npm run type-check passes
- npm run build succeeds with all pages compiling
- Combined with Plan 12-01: package.json has "motion", all 112 files import from "motion/react", LazyMotion wraps the app
</success_criteria>

<output>
After completion, create `.planning/phases/12-motion-v12-migration/12-02-SUMMARY.md`
</output>
