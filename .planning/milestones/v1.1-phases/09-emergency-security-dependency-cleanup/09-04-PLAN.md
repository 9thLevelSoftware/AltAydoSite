---
phase: 09-emergency-security-dependency-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - package.json
  - package-lock.json
  - src/scripts/migrate-users.ts
autonomous: true
must_haves:
  truths:
    - "npm audit shows zero critical vulnerabilities"
    - "Next.js version is 15.5.12 or higher"
    - "@azure/cosmos, @azure/identity, @azure/msal-node, azure-ad-verify-token, mammoth, openid-client, bcryptjs, @headlessui/react are not in package.json"
    - "@types/bcrypt and @types/nodemailer are in devDependencies (not dependencies)"
    - "@types/bcryptjs is completely removed"
    - "npm run build completes successfully"
    - "migrate-users.ts script is deleted"
  artifacts:
    - path: "package.json"
      provides: "Clean dependency list"
    - path: "package-lock.json"
      provides: "Updated lock file"
  key_links:
    - from: "package.json"
      to: "next"
      via: "version field"
      pattern: "\"next\": \"\\^?15\\.5"
    - from: "package.json"
      to: "eslint-config-next"
      via: "version field matching next"
      pattern: "\"eslint-config-next\": \"\\^?15\\.5"
---

<objective>
Remove 8 unused npm packages, move @types/* to devDependencies, delete the legacy migration script, and upgrade Next.js from 15.3.3 to 15.5.12 to patch CVE-2025-55182 (CVSS 10.0 RCE).

Purpose: Eliminate 23 high-severity transitive vulnerabilities from @azure/cosmos, remove the critical Next.js RCE vulnerability, and clean up the dependency tree.

Output: Clean package.json with zero critical/high vulnerabilities, upgraded Next.js, deleted legacy script.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-emergency-security-dependency-cleanup/09-RESEARCH.md

# Need Plan 01 summary to confirm diagnostic/force-fallback endpoints deleted
# Need Plan 02 summary to confirm bcryptjs is safe to remove (no imports)
@.planning/phases/09-emergency-security-dependency-cleanup/09-01-SUMMARY.md
@.planning/phases/09-emergency-security-dependency-cleanup/09-02-SUMMARY.md

@package.json
@src/scripts/migrate-users.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused packages, move @types/*, delete migration script</name>
  <files>
    package.json
    package-lock.json
    src/scripts/migrate-users.ts
  </files>
  <action>
1. DELETE `src/scripts/migrate-users.ts` -- This is the only file importing `@azure/cosmos`. The migration it performed is complete (Phase 8 consolidated all storage to MongoDB wire protocol). Keeping it would break after removing @azure/cosmos anyway.

2. REMOVE the `migrate-users` npm script from package.json scripts section (should already be done by Plan 01, but verify).

3. UNINSTALL 8 unused packages (run as single npm command):
   ```bash
   npm uninstall @azure/cosmos @azure/identity @azure/msal-node azure-ad-verify-token mammoth openid-client bcryptjs @headlessui/react
   ```

   Why each is safe to remove:
   - `@azure/cosmos`: Only import was in migrate-users.ts (being deleted). Source of 23 high-severity transitive vulns.
   - `@azure/identity`: Zero imports anywhere in src/
   - `@azure/msal-node`: Zero imports anywhere in src/
   - `azure-ad-verify-token`: Zero imports anywhere in src/
   - `mammoth`: Zero imports anywhere in src/
   - `openid-client`: Zero imports anywhere in src/
   - `bcryptjs`: Zero imports in src/. All auth files use `import bcrypt from 'bcrypt'` (native addon). Plan 02 confirmed OAuth hash fix doesn't introduce bcryptjs usage.
   - `@headlessui/react`: Zero imports anywhere in src/

4. REMOVE `@types/bcryptjs` entirely (the package it types is being removed):
   ```bash
   npm uninstall @types/bcryptjs
   ```

5. MOVE `@types/bcrypt` and `@types/nodemailer` to devDependencies:
   ```bash
   npm uninstall @types/bcrypt @types/nodemailer
   npm install --save-dev @types/bcrypt @types/nodemailer
   ```

6. VERIFY no remaining imports of removed packages:
   ```bash
   grep -rn "@azure/cosmos\|@azure/identity\|@azure/msal-node\|azure-ad-verify-token\|mammoth\|openid-client\|bcryptjs\|@headlessui/react" src/ --include="*.ts" --include="*.tsx"
   ```
   Should return zero matches.
  </action>
  <verify>
- `src/scripts/migrate-users.ts` does not exist
- `npm ls @azure/cosmos` returns "empty" / not found
- `npm ls bcryptjs` returns "empty" / not found
- Grep src/ for any import of removed packages returns zero matches
- `@types/bcrypt` appears in devDependencies (not dependencies) in package.json
- `@types/nodemailer` appears in devDependencies (not dependencies) in package.json
- `@types/bcryptjs` does not appear anywhere in package.json
- `npm run type-check` passes
  </verify>
  <done>
8 unused packages removed, @types/bcryptjs deleted, @types/bcrypt and @types/nodemailer in devDependencies, migrate-users.ts deleted, zero remaining imports of removed packages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Next.js to 15.5.12 and verify build</name>
  <files>
    package.json
    package-lock.json
  </files>
  <action>
1. UPGRADE Next.js and its companion packages:
   ```bash
   npm install next@15.5.12 eslint-config-next@15.5.12
   ```

   Why 15.5.12 and not 16.x: This is a minor version bump within v15 with no breaking API changes. 16.x is a major version requiring React 19 and async API changes -- out of scope for a security patch phase.

   Critical CVEs fixed:
   - CVE-2025-55182 (CVSS 10.0): Pre-auth RCE in React Server Components
   - CVE-2025-55183: Source code exposure
   - CVE-2025-55184: DoS vulnerability

2. VERIFY the upgrade:
   ```bash
   npm ls next        # Should show 15.5.12
   npm run build      # Must complete successfully
   ```

3. CHECK for breaking changes:
   - If build fails with webpack errors related to discord.js, check that `serverExternalPackages` config in `next.config.js` still works (it should -- the config API hasn't changed in 15.x minor releases)
   - If build fails with other errors, check the Next.js 15.4 and 15.5 release notes for any required config changes
   - The existing `output: 'standalone'` and `images: { unoptimized: true }` configs should be unaffected

4. RUN npm audit to verify vulnerability status:
   ```bash
   npm audit
   ```
   Expected: Zero critical vulnerabilities. The remaining moderate vulnerability in discord.js/undici is expected and out of scope (requires major version change).
  </action>
  <verify>
- `npm ls next` shows 15.5.12
- `npm ls eslint-config-next` shows 15.5.12
- `npm run build` completes successfully (check page count -- should be ~67 pages, 2 fewer due to deleted endpoints from Plan 01)
- `npm audit` shows zero critical and zero high vulnerabilities
- `npm run type-check` passes
  </verify>
  <done>
Next.js upgraded to 15.5.12, eslint-config-next matches. Build passes. npm audit shows zero critical/high vulnerabilities. CVE-2025-55182 (RCE) is patched.
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. `npm run build` completes successfully
3. `npm audit` shows zero critical and zero high vulnerabilities
4. `npm ls next` shows 15.5.12
5. No imports of removed packages exist in src/
6. @types/* packages in correct location (devDependencies)
7. migrate-users.ts does not exist
</verification>

<success_criteria>
- Next.js 15.5.12 installed and building successfully
- 8 unused packages removed from package.json
- @types/bcryptjs removed entirely, @types/bcrypt and @types/nodemailer in devDependencies
- migrate-users.ts deleted
- npm audit: zero critical, zero high vulnerabilities
- All existing pages build and type-check correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-emergency-security-dependency-cleanup/09-04-SUMMARY.md`
</output>
