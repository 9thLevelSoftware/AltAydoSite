---
phase: 14-design-system-consolidation
plan: 07
type: execute
wave: 3
depends_on: ["14-02", "14-03", "14-04", "14-05", "14-06"]
files_modified:
  - src/app/globals.css
  - src/components/ui/mobiglas/MobiGlasButton.tsx
autonomous: true

must_haves:
  truths:
    - "The !important declaration is removed from .mg-button background in globals.css"
    - "MobiGlasButton variant backgrounds render correctly without !important override"
    - "The .mg-button CSS class is preserved (not deleted) for potential utility use"
    - "No raw mg-button usage remains in any TSX file (only MobiGlasButton component uses it internally)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Clean .mg-button CSS without !important"
  key_links:
    - from: "src/components/ui/mobiglas/MobiGlasButton.tsx"
      to: "src/app/globals.css"
      via: "mg-button CSS class in variant strings"
      pattern: "mg-button"
---

<objective>
Remove !important from .mg-button CSS background and verify the entire button consolidation is clean.

Purpose: DS-08 requires removing the !important that fights with MobiGlasButton variant backgrounds. This MUST happen last (Wave 3) because it can only be safely done after all raw mg-button usages are migrated to MobiGlasButton (Plans 02-04). Also serves as the final sweep to verify DS-02 is fully complete.

Output: Clean .mg-button CSS, verified button consolidation across entire codebase.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-system-consolidation/14-02-SUMMARY.md
@.planning/phases/14-design-system-consolidation/14-03-SUMMARY.md
@.planning/phases/14-design-system-consolidation/14-04-SUMMARY.md
@src/app/globals.css
@src/components/ui/mobiglas/MobiGlasButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove !important from .mg-button and clean up deprecated CSS button classes</name>
  <files>src/app/globals.css</files>
  <action>
  1. **Remove !important from .mg-button background** (line ~128 in globals.css):
     - Change `background: rgba(var(--mg-dark), 0.5) !important;` to `background: rgba(var(--mg-dark), 0.5);`
     - This is now safe because all raw `<button className="mg-button">` elements have been replaced with MobiGlasButton, which uses Tailwind classes for variant-specific backgrounds that will now properly override this base style via CSS specificity.

  2. **Clean up undefined/deprecated CSS button classes:**
     - Remove `.mg-button-secondary` CSS rule (line ~1131) if it exists — OperationDetailView no longer uses it (migrated to MobiGlasButton in Plan 02)
     - Do NOT add `.mg-button-primary` or `.mg-button-danger` CSS definitions — those raw class usages were replaced by MobiGlasButton
     - Do NOT remove `.mg-button-small` if it exists — check if anything still uses it first

  3. **Keep the .mg-button base CSS class** — MobiGlasButton's primary/secondary/accent/danger variants include `mg-button` in their className string. The CSS class provides base styles (font, tracking, transition, padding) that MobiGlasButton relies on.

  4. **Verify the CSS is clean:**
     - Search globals.css for any remaining `!important` on .mg-button rules
     - Ensure no orphaned CSS classes reference deleted components (HolographicButton, etc.)
  </action>
  <verify>Run `npm run type-check`. Grep globals.css for `mg-button.*!important` to confirm zero remaining. Grep all TSX files for raw `className="mg-button` or `className={\`mg-button` usage to confirm only MobiGlasButton.tsx references the class.</verify>
  <done>!important removed from .mg-button background. Deprecated CSS button classes cleaned up. .mg-button base class preserved for MobiGlasButton internal use.</done>
</task>

<task type="auto">
  <name>Task 2: Final sweep — verify complete button consolidation across codebase</name>
  <files>src/components/ui/mobiglas/MobiGlasButton.tsx</files>
  <action>
  This is a verification and minor cleanup task. Run comprehensive checks:

  1. **Verify no raw mg-button in TSX files:**
     - Grep all `.tsx` files for `className="mg-button` or `className={\`mg-button` or `"mg-button `
     - The ONLY file that should contain `mg-button` as a class string is MobiGlasButton.tsx (in its variant definitions)
     - If any stragglers are found, replace them with MobiGlasButton

  2. **Verify HolographicButton is deleted:**
     - Confirm `src/components/fleet-ops/mission-planner/HolographicButton.tsx` does not exist
     - Grep for any remaining imports of HolographicButton

  3. **Verify no undefined CSS classes remain:**
     - Grep for `mg-button-primary` and `mg-button-danger` in TSX files — should be zero

  4. **Verify MobiGlasButton variant backgrounds work without !important:**
     - In MobiGlasButton.tsx, verify that each variant className string includes both `mg-button` (for base styles) AND Tailwind hover:bg-[...] classes (for variant-specific hover backgrounds)
     - The Tailwind `hover:bg-[...]` classes should now win over `.mg-button:hover` CSS because Tailwind generates more specific selectors
     - If there's a specificity issue (unlikely with Tailwind v4), add inline background style to MobiGlasButton as a targeted fix

  5. **Update barrel exports if needed:**
     - Verify index.ts no longer exports HolographicButton (it shouldn't — it was never in the barrel)
     - Verify MobiGlasFormError is properly exported

  If no issues found, no file changes needed — just document the verification in the summary.
  </action>
  <verify>Run `npm run type-check`. All grep checks above return expected results (zero stragglers).</verify>
  <done>Complete button consolidation verified. Only MobiGlasButton exists for buttons across the entire codebase. !important removed. No raw mg-button, HolographicButton, or undefined CSS classes remain.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. `grep -r "mg-button" src/ --include="*.tsx"` only returns hits in MobiGlasButton.tsx
3. `grep -r "!important" src/app/globals.css` shows zero hits on .mg-button rules
4. `grep -r "HolographicButton" src/` returns zero results
5. `grep -r "mg-button-primary\|mg-button-danger" src/ --include="*.tsx"` returns zero results
6. All MobiGlasButton variants render correct colors (primary=cyan, secondary=blue, accent=bright cyan, danger=red, success=green, ghost=transparent, outline=bordered)
</verification>

<success_criteria>
DS-08 complete (!important removed). DS-02 verified complete (only MobiGlasButton exists). The .mg-button CSS class is clean and serves as a base for MobiGlasButton. No raw button styling, undefined CSS classes, or deprecated components remain. TypeScript passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-consolidation/14-07-SUMMARY.md`
</output>
