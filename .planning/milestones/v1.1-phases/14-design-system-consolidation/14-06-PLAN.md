---
phase: 14-design-system-consolidation
plan: 06
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/app/security/page.tsx
  - src/components/fleet-ops/missions/MissionDetail.tsx
  - src/components/user/UserProfileContent.tsx
  - src/components/ErrorNotification.tsx
autonomous: true

must_haves:
  truths:
    - "Escort submit button in security/page.tsx uses MobiGlasButton with isLoading spinner"
    - "Form-level errors in security page, MissionDetail, and UserProfileContent use MobiGlasFormError or useToast"
    - "ErrorNotification component is either deprecated with a comment or replaced by useToast usage in consuming components"
  artifacts:
    - path: "src/app/security/page.tsx"
      provides: "Security page with MobiGlasButton loading spinner on escort submit"
      contains: "isLoading"
  key_links:
    - from: "src/app/security/page.tsx"
      to: "@/components/ui/mobiglas"
      via: "import MobiGlasButton"
      pattern: "import.*MobiGlasButton.*from.*mobiglas"
---

<objective>
Wire loading states to remaining high-frequency async actions and unify error display patterns across forms.

Purpose: UX-07 requires loading spinners on escort submit, and DS-05 requires consistent 3-tier error display. This plan converts the escort submit button to MobiGlasButton with isLoading (UX-07), standardizes form-level error banners to MobiGlasFormError (DS-05 tier 2), and marks ErrorNotification as deprecated in favor of useToast (DS-05 tier 3).

Output: All high-frequency async actions have loading spinners. Error display follows the 3-tier system.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-system-consolidation/14-01-SUMMARY.md
@src/app/security/page.tsx
@src/components/ErrorNotification.tsx
@src/components/ui/mobiglas/MobiGlasFormError.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire loading states and MobiGlasButton to escort submit and remaining async actions</name>
  <files>src/app/security/page.tsx</files>
  <action>
  In security/page.tsx:

  1. **Escort submit button**: Replace raw `<button className="mg-button ..." disabled={isSubmitting}>` with:
     ```tsx
     <MobiGlasButton
       type="submit"
       variant="danger"
       isLoading={isSubmitting}
       disabled={isSubmitting}
     >
       Submit Escort Request
     </MobiGlasButton>
     ```
     - The existing `isSubmitting` state variable provides the loading state
     - Use variant="danger" since escort requests involve security/combat operations (or variant="primary" if the button is styled as a standard action — read the file to determine)

  2. **Any other raw mg-button usages** in security/page.tsx: Replace with MobiGlasButton (appropriate variant)

  3. **Error display**: If the page has hand-rolled error banners, replace with `<MobiGlasFormError message={error} />`

  4. Import MobiGlasButton and MobiGlasFormError from `@/components/ui/mobiglas`

  Do NOT modify form submission logic, validation, or API calls.
  </action>
  <verify>Run `npm run type-check`. Verify security page uses MobiGlasButton with isLoading for escort submit.</verify>
  <done>Escort submit uses MobiGlasButton with isLoading spinner. All raw mg-button usage eliminated from security page.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize form-level errors and deprecate ErrorNotification</name>
  <files>src/components/fleet-ops/missions/MissionDetail.tsx, src/components/user/UserProfileContent.tsx, src/components/ErrorNotification.tsx</files>
  <action>
  1. **MissionDetail.tsx**: If it has hand-rolled error display (inline error divs, custom error banners), replace with `<MobiGlasFormError message={errorMessage} />`. If it already uses useToast for errors, leave it.

  2. **UserProfileContent.tsx**: If it has inline error banners for profile save errors, replace with `<MobiGlasFormError message={error} />` for form-level errors and confirm useToast is used for async save feedback. Profile save already uses MobiGlasButton with isLoading (confirmed in research).

  3. **ErrorNotification.tsx**: Add a deprecation comment at the top:
     ```typescript
     /**
      * @deprecated Use useToast() for system-level notifications or MobiGlasFormError for form-level errors.
      * This component is kept for backward compatibility but should not be used in new code.
      * Planned for removal in Phase 15.
      */
     ```
     Do NOT delete it yet — some components may still reference it. Just mark deprecated.

  4. For each file modified, import MobiGlasFormError from `@/components/ui/mobiglas` if needed.

  **3-tier error system verification after this task:**
  - Tier 1 (field-level): MobiGlasInput `error` prop (already exists)
  - Tier 2 (form-level): MobiGlasFormError component (created in Plan 01, wired here)
  - Tier 3 (system-level): useToast() hook (already exists from Phase 11)
  </action>
  <verify>Run `npm run type-check`. Verify ErrorNotification has @deprecated JSDoc comment. Verify MissionDetail and UserProfileContent use MobiGlasFormError or useToast for errors.</verify>
  <done>Form-level errors use MobiGlasFormError consistently. ErrorNotification is deprecated. 3-tier error system is complete: MobiGlasInput (field), MobiGlasFormError (form), useToast (system).</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. Escort submit in security/page.tsx uses MobiGlasButton with isLoading
3. MissionDetail and UserProfileContent use MobiGlasFormError or useToast for error display
4. ErrorNotification.tsx has @deprecated JSDoc comment
5. No new raw mg-button usage introduced
</verification>

<success_criteria>
All high-frequency async actions (mission create, profile save, escort submit, contact form, operation save) have loading spinners in their trigger buttons (UX-07 complete). Error display follows the 3-tier system: MobiGlasInput for fields, MobiGlasFormError for form banners, useToast for system notifications (DS-05 complete). ErrorNotification deprecated.
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-consolidation/14-06-SUMMARY.md`
</output>
