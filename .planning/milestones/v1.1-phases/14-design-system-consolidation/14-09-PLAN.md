---
phase: 14-design-system-consolidation
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/contact/ContactForm.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "ContactForm submit button uses MobiGlasButton's isLoading prop instead of custom spinner"
    - "Loading state shows the same spinner animation as LoginForm and SignupForm"
    - "Button text does not change during loading (isLoading replaces content with built-in spinner)"
  artifacts:
    - path: "src/components/contact/ContactForm.tsx"
      provides: "MobiGlasButton with isLoading prop for consistent loading state"
      contains: "isLoading={isLoading}"
  key_links:
    - from: "src/components/contact/ContactForm.tsx"
      to: "MobiGlasButton"
      via: "isLoading prop"
      pattern: "isLoading=\\{isLoading\\}"
---

<objective>
Replace ContactForm's custom loading spinner implementation with MobiGlasButton's built-in isLoading prop.

Purpose: Close Gap 2 from VERIFICATION.md -- high-frequency async actions must show loading spinners via MobiGlasButton's isLoading prop for consistency with LoginForm and SignupForm (Success Criterion 5).

Output: ContactForm uses isLoading prop, matching the design system pattern.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/mobiglas/MobiGlasButton.tsx
@src/components/contact/ContactForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace custom spinner with isLoading prop on ContactForm submit button</name>
  <files>src/components/contact/ContactForm.tsx</files>
  <action>
    Find the MobiGlasButton submit button (~lines 163-184):

    ```tsx
    <MobiGlasButton
      type="submit"
      variant="primary"
      size="lg"
      fullWidth
      withScanline
      disabled={isLoading}
      rightIcon={
        isLoading ? (
          <svg className="animate-spin h-4 w-4" ...>...</svg>
        ) : (
          <svg ...arrow icon...>...</svg>
        )
      }
    >
      {isLoading ? 'TRANSMITTING...' : 'TRANSMIT MESSAGE'}
    </MobiGlasButton>
    ```

    Replace with:

    ```tsx
    <MobiGlasButton
      type="submit"
      variant="primary"
      size="lg"
      fullWidth
      withScanline
      isLoading={isLoading}
      rightIcon={
        <svg xmlns="http://www.w3.org/2000/svg" className="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M14 5l7 7m0 0l-7 7m7-7H3" />
        </svg>
      }
    >
      TRANSMIT MESSAGE
    </MobiGlasButton>
    ```

    Key changes:
    1. Remove `disabled={isLoading}` -- MobiGlasButton's isLoading already sets `disabled={disabled || isLoading}` internally
    2. Add `isLoading={isLoading}` -- triggers MobiGlasButton's built-in spinner (motion.div with rotating border animation)
    3. Remove the rightIcon ternary -- just show the arrow icon always (MobiGlasButton hides rightIcon and children when isLoading is true, replacing them with its spinner)
    4. Remove the children text ternary -- just use static "TRANSMIT MESSAGE" (the built-in spinner replaces the text during loading)
  </action>
  <verify>
    Run `npm run type-check` to verify no TypeScript errors.
    Grep for custom spinner: `grep -n "animate-spin\|TRANSMITTING" src/components/contact/ContactForm.tsx` should return zero matches.
    Grep for isLoading prop: `grep -n "isLoading={isLoading}" src/components/contact/ContactForm.tsx` should return exactly 1 match.
  </verify>
  <done>ContactForm submit button uses MobiGlasButton's built-in isLoading prop. No custom spinner SVG, no text change during loading. Consistent with LoginForm and SignupForm patterns.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. No custom spinner SVG (`animate-spin`) in ContactForm
3. No conditional text (`TRANSMITTING...`) in ContactForm
4. `isLoading={isLoading}` prop present on submit button
5. `npm run build` succeeds
</verification>

<success_criteria>
- ContactForm loading state uses MobiGlasButton isLoading prop
- Custom spinner implementation fully removed
- Consistent loading UX with LoginForm and SignupForm
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-consolidation/14-09-SUMMARY.md`
</output>
