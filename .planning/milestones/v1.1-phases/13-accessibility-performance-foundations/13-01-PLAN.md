---
phase: 13-accessibility-performance-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/Navigation.tsx
  - src/app/dashboard/page.tsx
  - next.config.js
autonomous: true
must_haves:
  truths:
    - "Keyboard-focused elements show cyan glow; mouse clicks do not trigger focus outlines"
    - "Static assets under /_next/static return Cache-Control: immutable with max-age=31536000"
    - "A skip-to-content link is visible on keyboard focus and jumps to main content"
    - "Mobile menu closes automatically on route change"
    - "Dashboard loads without artificial 800ms authentication delay"
  artifacts:
    - path: "src/app/globals.css"
      provides: "focus-visible styles replacing *:focus rule"
      contains: "focus-visible"
    - path: "src/app/layout.tsx"
      provides: "Skip-to-content link and main id"
      contains: "skip-to-content"
    - path: "next.config.js"
      provides: "Immutable cache header for static assets"
      contains: "immutable"
  key_links:
    - from: "src/app/globals.css"
      to: "all interactive elements"
      via: "focus-visible pseudo-class"
      pattern: "focus-visible"
    - from: "src/app/layout.tsx"
      to: "main content area"
      via: "skip link href=#main-content"
      pattern: "main-content"
---

<objective>
Replace global focus styles with keyboard-only focus-visible indicators, add skip-to-content accessibility link, auto-close mobile menu on navigation, remove dashboard auth delay, and set immutable cache headers for static assets.

Purpose: Addresses UX-06, UX-09, UX-10, PERF-01, PERF-07 -- the quick-win accessibility and performance fixes that don't require structural changes.
Output: Updated globals.css, layout.tsx, Navigation.tsx, dashboard/page.tsx, next.config.js
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-accessibility-performance-foundations/13-RESEARCH.md
@src/app/globals.css
@src/app/layout.tsx
@src/components/Navigation.tsx
@src/app/dashboard/page.tsx
@next.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace focus styles with focus-visible and add skip-to-content link</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
  **globals.css changes:**
  1. Find the existing `*:focus` rule (around line 437) that applies `outline: none; box-shadow: 0 0 0 1px rgba(var(--mg-primary), 0.3);` to all elements.
  2. Replace it with TWO rules:
     - `*:focus { outline: none; }` -- removes default outline for all focus
     - `*:focus-visible { outline: none; box-shadow: 0 0 0 2px rgba(var(--mg-accent), 0.6); }` -- cyan glow on keyboard focus only
  3. Find ALL `.mg-button:hover, .mg-button:focus` rules and change `:focus` to `:focus-visible` in each. Leave `:hover` unchanged.
  4. For `.mg-input:focus` and `.mg-select:focus` -- KEEP these as `:focus` (NOT `:focus-visible`). Inputs should show focus state even on mouse click per user decision.
  5. Search for any other `:focus` pseudo-class on non-input elements (buttons, links, nav items, tabs) and update to `:focus-visible`.

  **layout.tsx changes:**
  1. Add a skip-to-content link as the first child inside `<body>`, before any header/nav:
     ```tsx
     <a
       href="#main-content"
       className="sr-only focus:not-sr-only focus:fixed focus:top-2 focus:left-2 focus:z-[9999] focus:px-4 focus:py-2 focus:bg-[rgba(var(--mg-panel-dark),0.95)] focus:text-[rgba(var(--mg-accent),1)] focus:border focus:border-[rgba(var(--mg-accent),0.6)] focus:rounded-sm focus:text-sm focus:font-quantify"
     >
       Skip to main content
     </a>
     ```
  2. Find the `<main>` tag in layout.tsx and add `id="main-content"` to it. If there's no `<main>` tag, add `id="main-content"` to the primary content wrapper that follows the navigation.
  </action>
  <verify>
  - `npm run build` completes without errors
  - Grep globals.css for `focus-visible` -- should find the new rule
  - Grep globals.css for `*:focus {` -- should exist with just `outline: none`
  - Grep layout.tsx for `skip-to-content` or `main-content` -- should find both
  - Verify `.mg-input:focus` and `.mg-select:focus` remain as `:focus` (not changed to `:focus-visible`)
  </verify>
  <done>
  - `*:focus-visible` rule exists with `box-shadow: 0 0 0 2px rgba(var(--mg-accent), 0.6)`
  - `*:focus` rule exists with only `outline: none` (no box-shadow)
  - All `.mg-button` focus rules use `:focus-visible`
  - All `.mg-input` and `.mg-select` focus rules still use `:focus`
  - Skip-to-content link exists as first focusable element in layout
  - `<main>` or primary content wrapper has `id="main-content"`
  </done>
</task>

<task type="auto">
  <name>Task 2: Auto-close mobile menu on route change, remove auth delay, set cache headers</name>
  <files>src/components/Navigation.tsx, src/app/dashboard/page.tsx, next.config.js</files>
  <action>
  **Navigation.tsx (UX-10):**
  1. Import `usePathname` from `next/navigation` (if not already imported).
  2. Add a `useEffect` that watches `pathname` changes and calls `setIsOpen(false)` (or equivalent mobile menu close function):
     ```tsx
     const pathname = usePathname();
     useEffect(() => {
       setIsOpen(false);
     }, [pathname]);
     ```
  3. This handles programmatic navigation (e.g., `router.push`) that doesn't trigger onClick handlers on Links. The existing `onClick={() => setIsOpen(false)}` on individual links handles direct clicks.

  **dashboard/page.tsx (PERF-07):**
  1. Find the `setTimeout(..., 800)` on approximately line 45 that artificially delays the authentication check.
  2. Remove the setTimeout wrapper entirely. Use the session status directly:
     - If there's a pattern like `setTimeout(() => { setLoading(false) }, 800)`, replace with immediate `setLoading(false)` when session status resolves.
     - If the timeout guards a redirect, use the session `status` from `useSession()` directly (status === 'loading' for pending, status === 'authenticated' for ready, status === 'unauthenticated' for redirect).
  3. Preserve any loading state UI -- just make it respond to actual session status instead of a fixed timer.

  **next.config.js (PERF-01):**
  1. Find the existing `headers()` configuration.
  2. Locate the `/_next/static/:path*` source entry.
  3. Change its Cache-Control value from `public, max-age=3600` (1 hour) to `public, immutable, max-age=31536000` (1 year).
  4. Leave all other header entries unchanged (fonts, images, API routes).

  **UX-08 (Dashboard fake metrics):**
  1. Check `src/app/dashboard/page.tsx` and dashboard sub-pages (`/dashboard/archives/*`, `/dashboard/subsidiaries/*`) for hardcoded sample/demo data.
  2. If `archives/hierarchy/page.tsx` has "Sample organizational data structures" or similar, add a visible "Demo Data" label (e.g., a small badge or text indicator).
  3. If no fake metrics are found in the main dashboard, document that UX-08 is satisfied (the dashboard shows real event data from Discord).
  </action>
  <verify>
  - `npm run build` completes without errors
  - Grep Navigation.tsx for `usePathname` and `setIsOpen(false)` in a useEffect
  - Grep dashboard/page.tsx -- no `setTimeout` with 800ms delay present
  - Grep next.config.js for `immutable` -- should appear in /_next/static header
  - Grep next.config.js for `max-age=31536000` -- should appear exactly once
  </verify>
  <done>
  - Mobile menu closes on programmatic route changes via usePathname effect
  - Dashboard authentication check has no artificial delay (setTimeout removed)
  - `/_next/static` assets return `Cache-Control: public, immutable, max-age=31536000`
  - UX-08 either has "Demo Data" labels added or is documented as already satisfied
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no new errors
2. `npm run type-check` passes
3. Focus-visible: Grep confirms `*:focus-visible` in globals.css with cyan glow box-shadow
4. Skip link: Grep confirms `main-content` ID on main element and skip link in layout
5. Mobile menu: usePathname effect present in Navigation.tsx
6. Auth delay: No setTimeout with 800 in dashboard/page.tsx
7. Cache: next.config.js has immutable header for /_next/static
</verification>

<success_criteria>
- Keyboard Tab shows cyan glow on buttons/links; mouse click does not show focus outline on buttons (inputs still show focus on click)
- Skip-to-content link visible when user tabs from address bar
- Mobile nav closes when navigating via programmatic routes
- Dashboard page loads without 800ms artificial delay
- Static assets served with 1-year immutable cache header
</success_criteria>

<output>
After completion, create `.planning/phases/13-accessibility-performance-foundations/13-01-SUMMARY.md`
</output>
