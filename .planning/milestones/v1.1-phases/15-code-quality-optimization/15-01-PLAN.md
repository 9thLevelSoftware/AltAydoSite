---
phase: 15-code-quality-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/storage-errors.ts
  - src/lib/user-storage.ts
  - src/lib/planned-mission-storage.ts
  - src/lib/operation-storage.ts
  - src/lib/mission-storage.ts
  - src/lib/resource-storage.ts
  - src/lib/escort-request-storage.ts
  - src/lib/mission-template-storage.ts
autonomous: true

must_haves:
  truths:
    - "When two users edit the same planned mission concurrently, the second save is rejected with a conflict error instead of silently overwriting"
    - "Optimistic locking works identically across all 7 storage modules (users + 6 new)"
    - "StaleDocumentError is importable from a shared module without coupling to user-storage"
    - "Existing API callers that don't send expectedVersion continue to work (backward compatible)"
  artifacts:
    - path: "src/lib/storage-errors.ts"
      provides: "Shared StaleDocumentError class"
      exports: ["StaleDocumentError"]
    - path: "src/lib/planned-mission-storage.ts"
      provides: "Optimistic locking on update functions"
      contains: "$inc.*__v"
    - path: "src/lib/operation-storage.ts"
      provides: "Optimistic locking on update functions"
      contains: "$inc.*__v"
    - path: "src/lib/mission-storage.ts"
      provides: "Optimistic locking on update functions"
      contains: "$inc.*__v"
    - path: "src/lib/resource-storage.ts"
      provides: "Optimistic locking on update functions"
      contains: "$inc.*__v"
    - path: "src/lib/escort-request-storage.ts"
      provides: "Optimistic locking on update functions"
      contains: "$inc.*__v"
    - path: "src/lib/mission-template-storage.ts"
      provides: "Optimistic locking on update functions"
      contains: "$inc.*__v"
  key_links:
    - from: "src/lib/planned-mission-storage.ts"
      to: "src/lib/storage-errors.ts"
      via: "import StaleDocumentError"
      pattern: "import.*StaleDocumentError.*storage-errors"
    - from: "src/lib/user-storage.ts"
      to: "src/lib/storage-errors.ts"
      via: "import StaleDocumentError (migrated from inline)"
      pattern: "import.*StaleDocumentError.*storage-errors"
---

<objective>
Add optimistic locking with `__v` version field to all 6 remaining storage modules, replicating the proven pattern from user-storage.ts. Extract StaleDocumentError to a shared module first.

Purpose: QUAL-03 requires that concurrent edits to the same resource are detected and rejected instead of silently overwriting. user-storage.ts already has this pattern -- this plan extends it to planned-mission-storage, operation-storage, mission-storage, resource-storage, escort-request-storage, and mission-template-storage.

Output: Shared storage-errors.ts module, 6 storage modules with `__v` support, user-storage.ts updated to import from shared module.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-code-quality-optimization/15-RESEARCH.md
@src/lib/user-storage.ts
@src/lib/planned-mission-storage.ts
@src/lib/operation-storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract StaleDocumentError to shared module and update user-storage import</name>
  <files>src/lib/storage-errors.ts, src/lib/user-storage.ts</files>
  <action>
  1. Create `src/lib/storage-errors.ts` with the `StaleDocumentError` class extracted from user-storage.ts:
     ```typescript
     export class StaleDocumentError extends Error {
       public collection: string;
       public documentId: string;
       constructor(collection: string, documentId: string) {
         super(`Document ${documentId} in ${collection} has been modified by another process`);
         this.name = 'StaleDocumentError';
         this.collection = collection;
         this.documentId = documentId;
       }
     }
     ```
     Match the exact constructor signature currently used in user-storage.ts.

  2. Update `src/lib/user-storage.ts`:
     - Remove the inline `StaleDocumentError` class definition
     - Add `import { StaleDocumentError } from '@/lib/storage-errors';`
     - Also add a re-export for backward compat: `export { StaleDocumentError } from '@/lib/storage-errors';`
     - Verify all existing usage of StaleDocumentError in user-storage still works (the throw sites and the profile route that catches it)

  Do NOT change any user-storage update logic, version checking, or `__v` behavior.
  </action>
  <verify>Run `npm run type-check` to confirm no import errors. Verify `src/app/api/profile/route.ts` still imports StaleDocumentError from user-storage (backward compat re-export).</verify>
  <done>StaleDocumentError lives in storage-errors.ts, user-storage.ts imports from there and re-exports for backward compat. No behavioral changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add optimistic locking to all 6 remaining storage modules</name>
  <files>src/lib/planned-mission-storage.ts, src/lib/operation-storage.ts, src/lib/mission-storage.ts, src/lib/resource-storage.ts, src/lib/escort-request-storage.ts, src/lib/mission-template-storage.ts</files>
  <action>
  For EACH of the 6 storage modules, apply this pattern (copied from user-storage.ts):

  1. Add `import { StaleDocumentError } from '@/lib/storage-errors';` at the top

  2. Find each `update*()` function (e.g., `updatePlannedMission`, `updateOperation`, `updateMission`, `updateResource`, `updateEscortRequest`, `updateMissionTemplate`). For each:
     - Add `expectedVersion?: number` as the LAST parameter (optional for backward compat)
     - Build version filter before the findOneAndUpdate call:
       ```typescript
       const versionFilter: Record<string, unknown> = {};
       if (expectedVersion !== undefined) {
         if (expectedVersion === 0) {
           versionFilter.$or = [{ __v: 0 }, { __v: { $exists: false } }];
         } else {
           versionFilter.__v = expectedVersion;
         }
       }
       ```
     - In the `findOneAndUpdate` call, spread `...versionFilter` into the filter alongside the existing `{ id }` filter
     - Add `$inc: { __v: 1 }` to the update operations
     - Strip `__v` from the update data to prevent `$set`/`$inc` conflict:
       ```typescript
       const { id: _id, __v: _v, ...updateFields } = updateData as any;
       ```
     - After the findOneAndUpdate, if result is null AND expectedVersion was provided:
       ```typescript
       if (!result && expectedVersion !== undefined) {
         const exists = await collection.findOne({ id });
         if (exists) throw new StaleDocumentError('collection_name', id);
       }
       ```

  3. For `create*()` functions: add `__v: 0` to the initial document insert alongside other fields. This seeds the version for future updates.

  **Important:** Keep `expectedVersion` optional everywhere. When not provided, skip version checking entirely (existing behavior preserved). Do NOT add `__v` logic to local-storage fallback paths -- only MongoDB operations.

  **Storage modules and their update functions to modify:**
  - `planned-mission-storage.ts`: `updatePlannedMission()`, `createPlannedMission()`
  - `operation-storage.ts`: `updateOperation()`, `createOperation()`
  - `mission-storage.ts`: `updateMission()`, `createMission()`
  - `resource-storage.ts`: `updateResource()`, `createResource()`
  - `escort-request-storage.ts`: `updateEscortRequest()`, `createEscortRequest()`
  - `mission-template-storage.ts`: `updateMissionTemplate()`, `createMissionTemplate()`
  </action>
  <verify>Run `npm run type-check` to confirm no TypeScript errors across all 6 files. Grep for `StaleDocumentError` to verify all 6 import it. Grep for `$inc.*__v` to verify all 6 have version incrementing.</verify>
  <done>All 6 storage modules have optional `expectedVersion` parameter on update functions, `__v: 0` on create functions, and throw `StaleDocumentError` on version mismatch. Backward compat maintained -- callers without expectedVersion work unchanged.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes with no errors
2. `grep -r "StaleDocumentError" src/lib/` shows imports in all 7 storage modules (user + 6 new)
3. `grep -r "\$inc.*__v" src/lib/` shows version incrementing in all 7 storage modules
4. `grep -r "expectedVersion" src/lib/*-storage.ts` shows optional parameter in all update functions
5. No changes to local-storage.ts fallback paths
</verification>

<success_criteria>
All 7 storage modules (user + 6 new) support optimistic locking via `__v` field. StaleDocumentError is a shared module. Backward compatibility preserved -- existing API callers that don't send expectedVersion continue to work without version checking.
</success_criteria>

<output>
After completion, create `.planning/phases/15-code-quality-optimization/15-01-SUMMARY.md`
</output>
