---
phase: 15-code-quality-optimization
plan: 06
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - src/app/api/auth/signup/route.ts
  - src/app/api/auth/forgot-password/route.ts
  - src/app/api/auth/reset-password/route.ts
  - src/app/api/auth/auth.ts
  - src/app/api/profile/route.ts
  - src/app/api/planned-missions/route.ts
  - src/app/api/planned-missions/[id]/route.ts
  - src/app/api/planned-missions/[id]/attendance/route.ts
  - src/app/api/planned-missions/[id]/discord/route.ts
  - src/app/api/security/escort-requests/route.ts
  - src/app/api/fleet-ops/operations/route.ts
  - src/app/api/fleet-ops/operations/[id]/route.ts
  - src/app/api/fleet-ops/operations/upload-image/route.ts
  - src/app/api/fleet-ops/operations/images/[id]/route.ts
  - src/app/api/fleet-ops/resources/route.ts
  - src/app/api/fleet-ops/resources/[id]/route.ts
  - src/app/api/fleet-ops/resources/allocations/route.ts
  - src/app/api/fleet-ops/missions/route.ts
  - src/app/api/events/discord/route.ts
  - src/app/api/admin/discord-sync/route.ts
  - src/app/api/discord/roles/route.ts
  - src/app/api/discord/roles/user/route.ts
  - src/app/api/discord/init/route.ts
  - src/app/api/discord/assign-synced-role/route.ts
  - src/app/api/finance/transactions/route.ts
  - src/app/api/users/route.ts
  - src/app/api/users/leaders/route.ts
  - src/app/api/ships/route.ts
  - src/app/api/ships/[id]/route.ts
  - src/app/api/cron/warm-images/route.ts
  - src/app/api/fleet-ops/force-fallback/route.ts
autonomous: true

must_haves:
  truths:
    - "Zero console.log/warn/error calls remain in any API route file"
    - "All API routes use logger.info/warn/error from @/lib/logger"
    - "API error responses include structured logging with request context (method, path)"
    - "Auth-related routes do not log sensitive data (passwords, tokens)"
  artifacts:
    - path: "src/app/api/auth/signup/route.ts"
      provides: "Auth route with structured logging"
      contains: "import { logger }"
    - path: "src/app/api/planned-missions/route.ts"
      provides: "Missions route with structured logging"
      contains: "import { logger }"
    - path: "src/app/api/fleet-ops/operations/route.ts"
      provides: "Operations route with structured logging"
      contains: "import { logger }"
  key_links:
    - from: "src/app/api/auth/signup/route.ts"
      to: "src/lib/logger.ts"
      via: "import { logger }"
      pattern: "import.*logger.*from.*@/lib/logger"
---

<objective>
Replace all console.log/warn/error calls in API route files with structured logger calls. This covers ~31 route files with ~152 total console occurrences.

Purpose: QUAL-01 requires all server-side logging to use the structured Logger. API routes are the second-highest volume source after storage modules. This plan completes the API layer migration, and depends on Plan 15-02 because the fleet-ops missions route was modified for state machine validation.

Output: ~31 API route files using structured logger with zero console.* calls remaining.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-code-quality-optimization/15-RESEARCH.md
@.planning/phases/15-code-quality-optimization/15-02-SUMMARY.md
@src/lib/logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate logging in auth, profile, and planned-missions routes (15 files, ~80 calls)</name>
  <files>src/app/api/auth/signup/route.ts, src/app/api/auth/forgot-password/route.ts, src/app/api/auth/reset-password/route.ts, src/app/api/auth/auth.ts, src/app/api/profile/route.ts, src/app/api/planned-missions/route.ts, src/app/api/planned-missions/[id]/route.ts, src/app/api/planned-missions/[id]/attendance/route.ts, src/app/api/planned-missions/[id]/discord/route.ts, src/app/api/security/escort-requests/route.ts, src/app/api/fleet-ops/missions/route.ts, src/app/api/fleet-ops/operations/route.ts, src/app/api/fleet-ops/operations/[id]/route.ts, src/app/api/fleet-ops/operations/upload-image/route.ts, src/app/api/fleet-ops/operations/images/[id]/route.ts</files>
  <action>
  For each API route file, apply the logging migration:

  1. Add `import { logger } from '@/lib/logger';` at the top

  2. Replace ALL `console.log(...)` → `logger.info(...)`:
     - Convert to structured format with API context
     - Example: `console.log('Creating user:', email)` → `logger.info('Creating user', { email, route: '/api/auth/signup' })`

  3. Replace ALL `console.warn(...)` → `logger.warn(...)`:
     - Same structured format with context

  4. Replace ALL `console.error(...)` → `logger.error(...)`:
     - Pass Error objects: `logger.error('Signup failed', error instanceof Error ? error : new Error(String(error)), { route: '/api/auth/signup' })`

  **Security-sensitive routes (auth, profile):**
  - `signup/route.ts` (17 calls): Do NOT log passwords or password hashes. Log email, success/failure, validation errors.
  - `forgot-password/route.ts` (7 calls): Do NOT log reset tokens. Log email (redacted if desired), success/failure.
  - `reset-password/route.ts` (10 calls): Do NOT log tokens or new passwords. Log success/failure.
  - `auth.ts` (5 calls): Do NOT log credentials. Log auth attempt outcome.
  - `profile/route.ts` (8 calls): Safe to log profile field names but NOT values of sensitive fields.

  **Fleet-ops and missions routes:**
  - `planned-missions/route.ts` (15 calls): Log mission IDs, operations, status changes
  - `fleet-ops/missions/route.ts`: Already modified by Plan 15-02 for state machine. Add logger import and migrate console calls.
  - `escort-requests/route.ts` (14 calls): Log request IDs, status changes
  - `operations` routes: Log operation IDs, status changes, image operations

  Consider using `logger.apiLog()` for request/response logging where appropriate (it auto-selects severity based on status code).

  Do NOT change any business logic, validation, or response behavior.
  </action>
  <verify>Run `npm run type-check`. Grep for `console\.(log|warn|error)` in these 15 files -- should return 0 matches.</verify>
  <done>15 API route files have zero console.* calls and use structured logger. No sensitive data (passwords, tokens) in logs.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate logging in remaining API routes (16 files, ~72 calls)</name>
  <files>src/app/api/fleet-ops/resources/route.ts, src/app/api/fleet-ops/resources/[id]/route.ts, src/app/api/fleet-ops/resources/allocations/route.ts, src/app/api/fleet-ops/force-fallback/route.ts, src/app/api/events/discord/route.ts, src/app/api/admin/discord-sync/route.ts, src/app/api/discord/roles/route.ts, src/app/api/discord/roles/user/route.ts, src/app/api/discord/init/route.ts, src/app/api/discord/assign-synced-role/route.ts, src/app/api/finance/transactions/route.ts, src/app/api/users/route.ts, src/app/api/users/leaders/route.ts, src/app/api/ships/route.ts, src/app/api/ships/[id]/route.ts, src/app/api/cron/warm-images/route.ts</files>
  <action>
  Apply the same logging migration pattern to all remaining API routes:

  1. Add `import { logger } from '@/lib/logger';`
  2. Replace `console.log` → `logger.info` with structured context
  3. Replace `console.warn` → `logger.warn` with structured context
  4. Replace `console.error` → `logger.error` with Error object and context

  **Route-specific notes:**
  - `discord/*.ts` routes: Log Discord API interactions, role sync operations, event IDs. Do NOT log Discord tokens.
  - `admin/discord-sync/route.ts`: Log sync operation progress, user counts
  - `finance/transactions/route.ts` (1 call): Simple migration
  - `users/route.ts` (1 call), `users/leaders/route.ts` (2 calls): Do NOT log user passwords
  - `ships/route.ts` (1 call), `ships/[id]/route.ts` (1 call): Simple migration
  - `cron/warm-images/route.ts` (5 calls): Log cron execution, image counts
  - `fleet-ops/resources` routes: Log resource IDs, allocation changes
  - `fleet-ops/force-fallback/route.ts` (1 call): Log fallback toggle

  Do NOT change any business logic, validation, or response behavior.
  </action>
  <verify>Run `npm run type-check`. Grep for `console\.(log|warn|error)` across ALL `src/app/api/` files -- should return 0 matches total. This confirms complete API layer migration.</verify>
  <done>All ~31 API route files have zero console.* calls and use structured logger. Complete API layer migration achieved.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes with no errors
2. `grep -r "console\.\(log\|warn\|error\)" src/app/api/` returns zero matches
3. `grep -r "import.*logger" src/app/api/` shows logger import in all route files with logging
4. No sensitive data (passwords, tokens, secrets) appears in log messages
5. Error logs pass Error objects for stack trace capture
</verification>

<success_criteria>
All API route files use structured Logger with zero console.* calls remaining. Log messages include structured context (route path, operation, IDs). Auth routes do not log sensitive data. Error logs capture stack traces.
</success_criteria>

<output>
After completion, create `.planning/phases/15-code-quality-optimization/15-06-SUMMARY.md`
</output>
