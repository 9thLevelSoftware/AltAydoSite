---
phase: 15-code-quality-optimization
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/discord-user-sync.ts
  - src/lib/discord-role-monitor.ts
  - src/lib/discord-role-monitor-init.ts
  - src/lib/discord.ts
  - src/lib/email-service.ts
  - src/lib/mongo-indexes.ts
  - src/lib/mongodb.ts
  - src/lib/auth-guards.ts
  - src/lib/errorReporting.ts
  - src/lib/finance.ts
  - src/lib/storage-utils.ts
  - src/lib/file-validation.ts
  - src/lib/ship-name-matcher.ts
  - src/lib/ship-sync.ts
  - src/lib/fleetyards/client.ts
  - src/lib/timezone.ts
autonomous: true

must_haves:
  truths:
    - "Zero console.log/warn/error calls remain in any non-storage lib file (excluding logger.ts itself and scripts/)"
    - "All lib modules use logger.info/warn/error from @/lib/logger"
    - "Discord integration modules log API interactions with structured context"
    - "MongoDB connection module logs connection events with structured context"
  artifacts:
    - path: "src/lib/discord.ts"
      provides: "Discord module with structured logging"
      contains: "import { logger }"
    - path: "src/lib/mongodb.ts"
      provides: "MongoDB connection with structured logging"
      contains: "import { logger }"
    - path: "src/lib/email-service.ts"
      provides: "Email service with structured logging"
      contains: "import { logger }"
  key_links:
    - from: "src/lib/discord.ts"
      to: "src/lib/logger.ts"
      via: "import { logger }"
      pattern: "import.*logger.*from.*@/lib/logger"
    - from: "src/lib/mongodb.ts"
      to: "src/lib/logger.ts"
      via: "import { logger }"
      pattern: "import.*logger.*from.*@/lib/logger"
---

<objective>
Replace all console.log/warn/error calls in non-storage lib files with structured logger calls. This covers ~16 files with ~135 total console occurrences, completing the server-side logging migration.

Purpose: QUAL-01 requires zero console.log/warn/error calls in production server-side code. Plans 15-05 and 15-06 handle storage modules and API routes. This plan completes the migration by covering the remaining lib files: Discord integration, MongoDB connection, email service, auth guards, and utility modules.

Output: ~16 lib files using structured logger with zero console.* calls remaining. Combined with Plans 05 and 06, this achieves complete server-side logging migration.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-code-quality-optimization/15-RESEARCH.md
@src/lib/logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate logging in Discord, email, and MongoDB modules (8 files, ~95 calls)</name>
  <files>src/lib/discord-user-sync.ts, src/lib/discord-role-monitor.ts, src/lib/discord-role-monitor-init.ts, src/lib/discord.ts, src/lib/email-service.ts, src/lib/mongo-indexes.ts, src/lib/mongodb.ts, src/lib/fleetyards/client.ts</files>
  <action>
  For each file, apply the standard logging migration:

  1. Add `import { logger } from '@/lib/logger';`
  2. Replace `console.log` → `logger.info` with structured context
  3. Replace `console.warn` → `logger.warn` with structured context
  4. Replace `console.error` → `logger.error` with Error object and context

  **File-specific guidance:**

  - `discord-user-sync.ts` (21 calls): Log sync operations, user counts, role mappings. Add context: `{ module: 'discord-sync' }`. Do NOT log Discord tokens.
  - `discord-role-monitor.ts` (19 calls): Log monitor events, role changes, connection status. Add context: `{ module: 'discord-monitor' }`.
  - `discord-role-monitor-init.ts` (6 calls): Log initialization steps. Add context: `{ module: 'discord-monitor-init' }`.
  - `discord.ts` (15 calls): Log Discord API calls, guild operations. Add context: `{ module: 'discord' }`. Do NOT log bot tokens.
  - `email-service.ts` (5 calls): Log email send attempts, successes, failures. Do NOT log email content or passwords. Add context: `{ module: 'email' }`.
  - `mongo-indexes.ts` (10 calls): Log index creation operations, collection names. Add context: `{ module: 'mongo-indexes' }`.
  - `mongodb.ts` (9 calls): Log connection events, pool status, reconnection attempts. Add context: `{ module: 'mongodb' }`.
  - `fleetyards/client.ts` (12 calls): Log API requests to FleetYards, response status, sync counts. Add context: `{ module: 'fleetyards' }`.

  Do NOT change any business logic or error handling behavior.
  </action>
  <verify>Run `npm run type-check`. Grep for `console\.(log|warn|error)` in these 8 files -- should return 0 matches.</verify>
  <done>8 high-volume lib modules have zero console.* calls and use structured logger with module context.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate logging in remaining utility modules (8 files, ~20 calls)</name>
  <files>src/lib/auth-guards.ts, src/lib/errorReporting.ts, src/lib/finance.ts, src/lib/storage-utils.ts, src/lib/file-validation.ts, src/lib/ship-name-matcher.ts, src/lib/ship-sync.ts, src/lib/timezone.ts</files>
  <action>
  Apply the same logging migration to remaining lib utility files:

  1. Add `import { logger } from '@/lib/logger';`
  2. Replace all `console.log/warn/error` → structured logger calls

  **File-specific guidance:**

  - `auth-guards.ts` (2 calls): Log authorization failures. Add context: `{ module: 'auth-guards' }`.
  - `errorReporting.ts` (4 calls): Log error reporting events. Add context: `{ module: 'error-reporting' }`. NOTE: This file may use console.error intentionally as a reporting mechanism. Convert to logger.error but preserve the intent.
  - `finance.ts` (1 call): Simple migration. Add context: `{ module: 'finance' }`.
  - `storage-utils.ts` (5 calls): Log storage utility operations. Add context: `{ module: 'storage-utils' }`.
  - `file-validation.ts` (1 call): Simple migration. Add context: `{ module: 'file-validation' }`.
  - `ship-name-matcher.ts` (1 call): Simple migration. Add context: `{ module: 'ship-matcher' }`.
  - `ship-sync.ts` (13 calls): Log sync operations, ship counts, API responses. Add context: `{ module: 'ship-sync' }`.
  - `timezone.ts` (4 calls): Log timezone operations. Add context: `{ module: 'timezone' }`.

  **Exclusions (do NOT modify):**
  - `src/lib/logger.ts` -- the logger itself legitimately uses console.* internally as its output mechanism
  - `src/scripts/*` -- CLI tools appropriately use console.* for terminal output

  **Final verification sweep:** After both tasks, run a comprehensive grep across ALL of `src/lib/` (excluding logger.ts) to confirm zero remaining console.* calls.

  Do NOT change any business logic.
  </action>
  <verify>Run `npm run type-check`. Run comprehensive check: `grep -r "console\.\(log\|warn\|error\)" src/lib/ --include="*.ts" | grep -v logger.ts` -- should return 0 matches (excluding logger.ts which legitimately uses console). Scripts in src/scripts/ are explicitly excluded from this requirement.</verify>
  <done>All non-storage lib modules have zero console.* calls. Combined with Plans 05 and 06, the entire server-side codebase (src/lib/ + src/app/api/) uses structured logging. Only logger.ts itself and src/scripts/ retain console.* calls.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes with no errors
2. `grep -r "console\.\(log\|warn\|error\)" src/lib/ --include="*.ts" | grep -v logger.ts` returns zero matches
3. Combined with Plans 05 and 06: `grep -r "console\.\(log\|warn\|error\)" src/lib/ src/app/api/ --include="*.ts" | grep -v logger.ts` returns zero matches
4. All Discord/email modules do NOT log tokens or sensitive data
5. logger.ts itself is unchanged (still uses console.* internally as output)
6. src/scripts/ files are unchanged (CLI tools keep console.*)
</verification>

<success_criteria>
Complete server-side logging migration achieved across src/lib/ and src/app/api/. Zero console.* calls remain in production server-side code (excluding logger.ts internals and scripts/). All modules use structured Logger with severity levels, context objects, and Error stack traces.
</success_criteria>

<output>
After completion, create `.planning/phases/15-code-quality-optimization/15-07-SUMMARY.md`
</output>
