import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Debug Profile | AydoCorp',
};

export default function DebugProfileLayout({ children }: { children: React.ReactNode }) {
  return children;
}
