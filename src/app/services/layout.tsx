import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Services | AydoCorp',
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
