import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Recruitment Info | AydoCorp',
};

export default function RecruitmentInfoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
