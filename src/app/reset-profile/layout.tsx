import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Reset Profile | AydoCorp',
};

export default function ResetProfileLayout({ children }: { children: React.ReactNode }) {
  return children;
}
