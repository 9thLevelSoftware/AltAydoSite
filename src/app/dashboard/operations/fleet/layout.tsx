import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Fleet Operations | AydoCorp',
};

export default function FleetOperationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
