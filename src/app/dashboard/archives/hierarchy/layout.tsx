import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Org Hierarchy | AydoCorp',
};

export default function OrgHierarchyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
