import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Mission History | AydoCorp',
};

export default function MissionHistoryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
