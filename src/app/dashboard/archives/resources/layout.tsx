import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Resource Archives | AydoCorp',
};

export default function ResourceArchivesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
