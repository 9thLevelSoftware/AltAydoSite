import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Fleet Composition | AydoCorp',
};

export default function FleetCompositionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
