import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Empyrion Industries | AydoCorp',
};

export default function EmpyrionIndustriesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
