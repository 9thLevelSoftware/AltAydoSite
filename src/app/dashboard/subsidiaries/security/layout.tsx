import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Security Division | AydoCorp',
};

export default function SecurityDivisionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
