import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Certifications | AydoCorp',
};

export default function CertificationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
