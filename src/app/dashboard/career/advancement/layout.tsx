import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Career Advancement | AydoCorp',
};

export default function CareerAdvancementLayout({ children }: { children: React.ReactNode }) {
  return children;
}
