import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Fleet Database | AydoCorp',
};

export default function FleetDatabaseLayout({ children }: { children: React.ReactNode }) {
  return children;
}
